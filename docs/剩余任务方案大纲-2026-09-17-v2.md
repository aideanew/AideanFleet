# AideanFleet · 剩余任务方案大纲 v2（2026-09-17 复核版）
> 🔒 **ARCHIVED**（2026-09-24）：本文件为历史草稿（v2 草稿），已被 `docs/剩余任务与实施方案大纲-v1.md` 收敛取代。仅保留参考。


> 编制依据：全量重读 `docs/`（规划总览 / 契约 v1.1-v1.2 / adr / CLI安装决策单 / 验收清单-Phase2 / 部署指南
> / 剩余任务方案大纲-2026-09-17 v1）+ 三个穿透子代理对「核心引擎 / 集成链路 / 控制面与前端」的代码级复核
> + 全量测试实跑（2026-09-17）。每条任务附证据链（文件:行 / 实跑结果），可直接派工。
>
> 与 v1 的关系：v1 的 P0/P1 结论**逐条复验**，7 条成立、1 条口径更新（T-1.3 失败数 1→2）；
> 新增 v1 未覆盖的 9 项穿透发现（§0.3），并升级为独立任务 T-1.5 / T-2.5 / T-3.5。

---

## 0 · 分析基线（先看结论）

### 0.1 参考材料的理性与辩证取舍（复核版）

| 材料 | 复验结论 | 辩证判定 |
|---|---|---|
| 规划总览（M1/M2 融合决策表） | 分层铁律、DAG 心脏、MockExecutor 先行、Machine Gate 权威均已被代码证实落地 | ✅ 有效；其中「角色C 工作包」部分与实现存在**文档滞后**：mode 五值（代码含 plan）vs 文档四值、9 步启动 vs 文档 10 步交互引导——代码是对的，文档要回写（T-3.3） |
| 三份契约（状态机 v1.1 / 进度表字段 v1.2 / 控制台API v1.2） | 状态机 10 状态/14 迁移与 `state_machine.py` 逐条一致；db 字段 15+15+3 全覆盖且 `init_db()` 轻量迁移在位；server.py §0–§14 API 全集挂载 | ✅ 契约即实现成立；仅 health `version` 漂移（0.3.0 vs 示例 0.1.0，无害）；WS `stream_chunk` 为契约预留超集，**生产方缺失**（T-2.1） |
| adr.md（ADR-016~023） | 全部在代码中兑现：Vue3+Pinia、端口 5000、server.py 归角色A、派工单次非交互、契约层单向依赖 | ✅ 有效；ADR-017 端口 5000 因旧进程占用未归位（T-1.4，需拍板） |
| CLI 安装决策单（选项 A：FakeAdapter 主验收） | 已定稿；但两个未兑现后果仍真：①Manager 网关启动必败仍被谎报成功（T-1.2）；②真实 CLI 全链路 0 任务（T-2.4） | ⚠️ 有效但欠账未清；本轮新证：CLI 版本漂移已从 opencode 扩大到 **codex+opencode 双红**（T-1.3） |
| 验收清单-Phase2 | 仍标「修订中」，修订记录已定稿 202/6/0 基线但正文未回填 | ⚠️ 需定稿（T-3.2） |
| 剩余任务方案大纲 v1（2026-09-17） | §1 中 T-1.1/T-1.2/T-2.1/T-2.2/T-2.3/T-2.4/T-3.3 逐条复验**全部成立**（证据见下）；T-1.3 口径过期 | ✅ 主体可沿用，本文升级为 v2 |
| .workbuddy 记忆（2026-09-15/16） | R1~R9 历史阻塞复验：R1-R8 已解决，R9 ESCALATED 链路获新证据 | ✅ 已消化进本文，不单独保留 |

### 0.2 当前质量基线（2026-09-17 实测）

- 后端全量：`pytest tests/` → **210 passed / 2 failed / 3 skipped（114s）**。
  失败 = `tests/test_smoke.py::test_codex_smoke` + `test_opencode_smoke`（真实 CLI 外部依赖漂移，见 T-1.3）。
  ⚠️ 较 v1 记录的「212/1/2」**恶化 1 例**：codex 也红了，CLI 漂移在扩散。
- e2e：`tests-e2e/e2e-out/.last-run.json` → `status="failed"` 且 `failedTests=[]`（中断式失败）。
- git：2 个提交、无 tag；CI（tests.yml）在位。
- `data/fleet.db`：Test09161119（5 DONE + **7 ESCALATED**——升级链路真实跑出的实证）；TestTable0916（**4 ASSIGNED + 1 BLOCKED 残留**，属中断遗留，见 T-1.5）；AideanFleet 项目自身 `plan.json → "阶段": []`，0 真实任务。

### 0.3 本轮穿透新增发现（v1 未覆盖，均已固化为任务）

| # | 发现 | 证据 |
|---|---|---|
| E1 | events 并发保护仅进程内 `threading.Lock`，watchdog 等多进程写入 seq 可能冲突 | `fleet/core/events.py:126-130` |
| E2 | memory 条目 id 用 len+1 生成，删除后复用 `M-NNNN`，`memory_refs` 引用可能错位 | `fleet/core/memory.py::_next_id` |
| E3 | `retry.classify` 对响应文本做子串匹配，内容含 "402" 等字样会误判硬失败 | `fleet/models/retry.py:82-103` |
| E4 | 适配器错误码无独立 401（混入 400/TOOL_FAIL）；evidence attempt 恒硬编码 1；hermes prompt 走 argv 有截断风险 | `fleet/executors/base.py:32-66`、`claudecode.py:66`、`hermes.py` |
| E5 | 红线 `check_command` 能力就绪但**启动/派工链路零调用**；四种 mode 仅解析无执行分派（intake 落 PRD / audit 只读扫描 / discuss 圆桌未实现） | `redlines.py:47`、`hermes_entry.py:101` |
| E6 | notify 每日汇总定时器缺失；`watch_events_loop` 用行号当断点，事件轮转后漂移 | `triggers.py`、`smtp.py` |
| E7 | 治理层用量回路断裂：`model:call` 事件有产出（router）无消费（UsageAggregator.scan 零调用方）；ReportGenerator 未接线 | `usage.py:108,138`、`report.py:32-35` |
| E8 | launch_core 各 step `ok` 恒 True，health 结果不回写汇总 | `launch_core.py:328` |
| E9 | TestTable0916 残留 4 ASSIGNED + 1 BLOCKED；DOING 原样恢复语义（recovery.py）对该状态是否收敛未验证 | `data/fleet.db` 实测 + `fleet/rel/recovery.py` |

---

## 1 · 剩余待完成任务（每条带证据链）

> 判定口径：**P0 = 影响正确性/可信度，必须先修**；**P1 = 打通产品核心价值闭环**；**P2 = 加固与验收交付收口**。
> 每个原子任务给出：现状证据 → 改动落点 → 验收命令。

### T-1.1 【P0】启动链路健康检查与契约失配（v1 复验仍真）
- 证据链：`fleet/launcher/launch_core.py:147` 判 `data.get("status")=="ok"`；`fleet/console/server.py:270-273` 契约返回恰好四键 `version/db/events/config`，无 `status` 键；契约 §1 FROZEN。
- 后果：CLI/Hermes 启动的健康检查永远报「✗ 控制台健康检查失败」。
- 原子任务：
  - t1.1.1 改 `_health_check`：四键齐 + db/events/config 均真 → console=True（只改 launch_core，不动契约与 server.py）；
  - t1.1.2 `tests/test_cli_launch.py` 补「四键 mock→True / 缺键→False」两例；
  - t1.1.3 实跑 `python -m fleet.launcher.cli_start`（5050）日志出现「✓ 控制台健康检查通过」。
- 验收：`pytest tests/test_cli_launch.py -q` 全绿。

### T-1.2 【P0】Manager 网关(9900)「假成功」启动（v1 复验仍真）
- 证据链：`launch_core.py:119-135` Popen hermes 后无条件 `return True`；`launch_core.py:162-172` 探活依赖 `name=="Hermes-Manager"`；本机 PATH 无 hermes（决策单 A 档）。
- 原子任务：
  - t1.2.1 【用户拍板】三选一：(a) 接入已 clone 的 Hermes venv；(b) 本机 FastAPI manager-gateway stub（agent-card + 转发调度 API），解耦第三方 CLI；(c) 降级为如实报 `ok:false + reason`；
  - t1.2.2 `_start_manager_gateway` 改为「发命令 → 轮询探活 ≤5s → 以探活结果定 ok」；
  - t1.2.3 t1.1.1 的四键口径同步用于网关探活结果回写 steps。
- 验收：launch_core 返回的 `manager_gateway.ok` 与 9900 端口真实探活一致。

### T-1.3 【P0】双 CLI 冒烟红（v1 口径更新：1→2）
- 证据链：本轮实跑 `tests/test_smoke.py::test_codex_smoke` 与 `test_opencode_smoke` 双双 TOOL_FAIL（codex 0.153.4→0.154.0、opencode 1.18.31 输出格式漂移）；决策单「时点演进」既认风险正在兑现。
- 原子任务：
  - t1.3.1 分别单跑抓 stderr，逐个归因（CLI 升级漂移 vs 适配器解析缺陷）；
  - t1.3.2 优先做 `fleet/executors/codex.py` / `opencode.py` 解析宽口径兼容；无法兼容的用例标 `@pytest.mark.external_cli + skipif`（与 hermes 6 个条件跳过同口径）；
  - t1.3.3 失败模式回填 `fleet/models/retry.py` 口径验证。
- 验收：`pytest tests/ -q` → 0 failed。

### T-1.4 【P0·需拍板】端口 5000 归位（ADR-017 定版落地）
- 证据链：ADR-017 + 契约 §0 定版 5000；`.workbuddy/memory` 记录旧进程 PID 34196 占用（本轮未实测进程，执行前先复核）。
- 原子任务：t1.4.1 复核并经用户确认后停旧进程 → t1.4.2 `FLEET_CONSOLE_PORT=5000` 起 health 四键验证 → t1.4.3 e2e baseURL 切回 5000 → t1.4.4 清理 `reports/ui03r/switch-to-5000.sh` 临时口径。
- 验收：`curl http://127.0.0.1:5000/api/health` 返回本包 version。

### T-1.5 【P0·新增】TestTable0916 残留任务收敛验证（E9）
- 证据链：`data/fleet.db` 实测 4 ASSIGNED + 1 BLOCKED 残留；`fleet/rel/recovery.py:1-8` 四态续跑语义「绝不重复派工」。
- 原子任务：t1.5.1 对残留项目跑一次 `recover_all` 演练，验证 ASSIGNED 不重复派工、BLOCKED 等解除；t1.5.2 结论写入部署指南「中断恢复」一节；t1.5.3 决定残留数据清理或保留（用户确认）。
- 验收：recover_all 后无重复派工事件；残留任务状态有明确处置记录。

### T-2.1 【P1】stream_chunk 生产方落地（契约预留唯一未兑现项，v1 复验仍真）
- 证据链：契约 §8 标「预留」；本轮 grep `stream_chunk` 在 `fleet/**/*.py` **零命中**；前端已有注入验证通道。
- 原子任务：t2.1.1 契约 v1.3 草案（chunk 粒度/`taskId+chunk+seq`/与 chat_reply 关系，冻结后写码）→ t2.1.2 BaseAdapter 可选 `stream=True` + FakeAdapter 流式（Mock 先行）→ t2.1.3 server WS 广播 + REST 缓冲对称落 `chat_reply` → t2.1.4 `stream.spec.ts` 升级真实帧验证。
- 验收：Fake 流式执行期对话页逐帧增量；`tests/core/test_server_api.py` 新增用例绿。

### T-2.2 【P1】预算熔断 + 用量回灌接线（E7 并入，v1 复验仍真）
- 证据链：`budget.py:110,148` check/consume 熔断器实现且 28 例测试绿，但 `fleet/manager/` 零处 import governance；`UsageAggregator.scan` 零调用方（`usage.py:108,138`）；`model:call` 事件有产出无回灌；ReportGenerator 未接线。
- 原子任务：t2.2.1 契约 v1.3 补 `budget:exceeded` 后 run_loop 行为定义 → t2.2.2 `schedule_once` 派工前 `check()` 闸门（超限跳过派工 + 事件 + 审批单）→ t2.2.3 dispatcher 回执处 `consume(usage.total_tokens)` → t2.2.4 **新增**：事件消费处回灌 `record_usage`（打通 model:call→governance.db，成本面板出数）→ t2.2.5 ReportGenerator 暴露为 `/api/governance/report`（或并入 usage 面）→ t2.2.6 超限→审批→恢复全链路测试。
- 验收：超限后 `schedule_once` 不派工且事件流有 `budget:exceeded` + 审批单；批准后恢复；成本面板读到非零用量。

### T-2.3 【P1】审批门接入关键动作（v1 复验仍真）
- 证据链：`approval.py` 三场景+超时拒绝实现、`/api/approvals*` 已挂载，但派工/启动路径零消费点。
- 原子任务：t2.3.1 需审批动作清单定版（首版 2 项：预算超限放行、step 模式 BLOCKED 人工重派）→ t2.3.2 ControlBus 增加 approval 挂起维度 → t2.3.3 决定事件回流 run_loop + 服务端联通用例。
- 验收：审批中心点「通过」后被挂起动作真实继续。

### T-2.4 【P1】真实 CLI 全链路首跑（v1 复验仍真，且为 Phase 3 出口锚点）
- 证据链：AideanFleet 项目 `plan.json → "阶段": []`，全库无真实 CLI 产生的 DONE 任务；三个真实 CLI 本机在位（决策单）。
- 原子任务：t2.4.1 选最小真实任务走 `/api/intake` → `schedule_once` → t2.4.2 dispatcher→claudecode→四道门→六节报告→事件闭环实测 → t2.4.3 `reports/` 首个真实任务证据落盘 → t2.4.4 失败模式清单回填 retry 口径。
- 验收：`data/fleet.db` 出现 AideanFleet 项目一条 DONE 任务且带真实 evidence 目录。

### T-2.5 【P1·新增】红线接线 + mode 执行语义落地（E5）
- 证据链：`redlines.py:47 check_command` 仅测试调用，启动/派工链路零接线；`hermes_entry.py:101 map_mode_to_action` 仅文案映射，intake/audit/discuss 无执行分派（intake 实为 `fleet/manager/intake.py` 存在但未与 mode 分派打通——执行时核对）。
- 原子任务：t2.5.1 契约 v1.3 定义各 mode 的执行分派与红线拦截点 → t2.5.2 `_dispatch_mode` 接入 intake（PRD→plan）/audit（只读扫描报告）/discuss（圆桌摘要），run 走现有调度 → t2.5.3 红线拦截接入启动器命令执行与派工 prompt 构造两个入口，拦截写 `command:denied` 事件 → t2.5.4 单测：每 mode 一个解析+分派用例，红线命中/放行两例。
- 验收：四种 mode 各自产生对应产物（PRD/审计报告/讨论摘要）；`rm -rf` 类命令在接线点被拦截且有审计事件。

### T-3.1 【P2】e2e 全量干净复跑 + 归因
- 证据链：`.last-run.json` failed/空 failedTests（中断）。
- 原子任务：t3.1.1 T-1 修完后全量复跑（trace/screenshot/video off + 唯一 outputDir）→ t3.1.2 红用例逐条归因 → t3.1.3 结果回填验收清单。
- 验收：一轮全绿或每条红有归因。

### T-3.2 【P2】验收清单-Phase2 定稿 + Phase 3 清单编制
- 证据链：`docs/验收清单-Phase2.md:3-6` 仍「修订中」。
- 原子任务：t3.2.1 修订记录结论回填正文 → t3.2.2 新增 Phase 3 出口条目（本文 T-1~T-3）→ t3.2.3 经理复核定稿。
- 验收：清单无「修订中」字样。

### T-3.3 【P2】文档收口（含 v1 新增的「实现-文档偏差回写」）
- 证据链：README 14 字节；`docs/启动Hermes派工3(英文版).txt`、`启动Hermes派工4(中文版).txt` 0 字节（本轮实测）；`reports/INT-04R-evidence.md` 缺失；规划总览头部残留草稿；**新证**：INT-01 文档的 mode 四值/10 步引导与实现（五值/9 步 launch_core）不一致，`fleet/rel` 五件套在规划总览零记载。
- 原子任务：t3.3.1 README（定位+快速启动+测试命令+导览）→ t3.3.2 两份 0 字节文档补全或删除 → t3.3.3 拆出 INT-04R-evidence.md → t3.3.4 规划总览状态段刷新 + INT-01 偏差回写 + rel 模块补记 → t3.3.5 部署指南补「中断恢复」结论（承接 T-1.5）。
- 验收：`wc -c docs/*.txt` 无 0 字节；文档与实现口径一致。

### T-3.4 【P2·新增】引擎健壮性加固（E1/E2/E3/E4/E6/E8）
- 证据链：见 §0.3 E1/E2/E3/E4/E6/E8。
- 原子任务（独立小步，各自可回退）：
  - t3.4.1 events 写入加跨进程锁（Windows msvcrt / portalocker 或文件锁），并发单测补多进程用例；
  - t3.4.2 memory `_next_id` 改为「现有最大 id+1」，删除后不复用，补回归测试；
  - t3.4.3 `retry.classify` 收窄匹配域（仅对错误元数据/stderr 首段匹配），补误判用例；
  - t3.4.4 适配器补独立 `UNAUTHORIZED=401` 错误码 + evidence attempt 透传真实重试计数；
  - t3.4.5 hermes 适配器 prompt 改 stdin（对齐其余三家）；
  - t3.4.6 notify 补 `run_daily_summary` 定时器（挂 maintenance 线程）；`watch_events_loop` 断点改用事件 seq 字段；
  - t3.4.7 launch_core step.ok 以探活实测回写。
- 验收：每步对应单测绿；`pytest tests/ -q` 保持 0 failed。

### T-3.5 【P2】版本化与发布工程（v1 复验仍真）
- 证据链：2 commits、无 tag；部署指南 0.3.0；CI 在位。
- 原子任务：t3.5.1 全绿后打 tag v0.4.0（推送须经用户批准）→ t3.5.2 CI 补 e2e smoke job → t3.5.3 发布说明（六节报告口径）。
- 验收：`git tag` 存在 v0.4.0；CI 绿。

---

## 2 · 执行方案大纲（W → T → t 三级 + 逐 t 验收；W1 内串行，W2 双线并行，W3/W4 收口）

```markdown
AideanFleet 收口计划 v2

W1 · P0 正确性修复线（约 1 个工作日，串行；T-1.4/1.5 的拍板项尽早提出）
 ├─ T-1.1 启动健康检查契约失配
 │   ├─ t1.1.1 修 _health_check 四键口径（只动 launcher）        验收: pytest tests/test_cli_launch.py -q 绿
 │   ├─ t1.1.2 补 2 例 mock 用例                                 验收: 缺键→False
 │   └─ t1.1.3 cli_start@5050 实跑日志通过                       验收: 「✓ 控制台健康检查通过」
 ├─ T-1.2 Manager 网关假成功
 │   ├─ t1.2.1 【用户拍板】hermes venv / 本机 stub / 如实降级
 │   ├─ t1.2.2 探活后置（≤5s 轮询定 ok）                         验收: ok 与 9900 实况一致
 │   └─ t1.2.3 steps 汇总回写探活结果                            验收: 无谎报
 ├─ T-1.3 双 CLI 冒烟红（codex+opencode）
 │   ├─ t1.3.1 分别归因（stderr 抓取）                           验收: 归因结论落 reports/
 │   ├─ t1.3.2 解析宽口径修复（优先）或条件跳过                   验收: 用例独立绿
 │   └─ t1.3.3 全量 pytest 回 0 failed                           验收: pytest tests/ -q
 ├─ T-1.4 端口 5000 归位【依赖用户确认】
 │   ├─ t1.4.1 复核旧进程占用并经确认停止                         验收: 端口空闲
 │   ├─ t1.4.2 5000 起新控制台 health 四键                       验收: curl /api/health
 │   └─ t1.4.3 e2e baseURL 切回 + 临时脚本清理                    验收: e2e 冒烟绿
 └─ T-1.5 残留任务收敛（新增）
     ├─ t1.5.1 recover_all 演练                                   验收: 无重复派工事件
     ├─ t1.5.2 部署指南补「中断恢复」                              验收: 文档落盘
     └─ t1.5.3 残留数据处置【用户确认】                            验收: 处置记录

W2 · P1 核心价值闭环线（约 2-3 个工作日；T-2.1 与 T-2.2/2.3/2.5 可并行；契约 v1.3 冻结后再写码）
 ├─ T-2.1 stream_chunk 生产方
 │   ├─ t2.1.1 契约 v1.3 草案冻结                                验收: 字段/粒度定稿
 │   ├─ t2.1.2 BaseAdapter stream=True + Fake 流式               验收: Mock 先行铁律
 │   ├─ t2.1.3 WS 广播 + REST 对称 chat_reply                    验收: test_server_api 新例绿
 │   └─ t2.1.4 stream.spec.ts 真实帧验证                          验收: e2e 绿
 ├─ T-2.2 预算熔断 + 用量回灌接线
 │   ├─ t2.2.1 契约 v1.3 补 run_loop 熔断行为                     验收: 冻结
 │   ├─ t2.2.2 schedule_once 派工前 check 闸门                    验收: 超限不派工
 │   ├─ t2.2.3 dispatcher 回执 consume(usage)                    验收: 用量入账
 │   ├─ t2.2.4 model:call→record_usage 回灌                      验收: 成本面板非零
 │   ├─ t2.2.5 ReportGenerator 接 HTTP 面                        验收: /api 可读
 │   └─ t2.2.6 超限→审批→恢复全链路测试                            验收: 单测+e2e
 ├─ T-2.3 审批门接入关键动作
 │   ├─ t2.3.1 动作清单定版（2 项）                               验收: 文档
 │   ├─ t2.3.2 ControlBus approval 维度                           验收: 挂起/恢复语义
 │   └─ t2.3.3 决定回流 + 联通用例                                验收: 审批中心驱动调度
 ├─ T-2.4 真实 CLI 全链路首跑
 │   ├─ t2.4.1 最小真实任务 intake                               验收: 任务创建
 │   ├─ t2.4.2 claudecode→四道门→报告→闭环                        验收: DONE+evidence
 │   ├─ t2.4.3 reports/ 证据落盘                                  验收: 锚点文件
 │   └─ t2.4.4 失败模式回填 retry 口径                            验收: 清单文档
 └─ T-2.5 红线接线 + mode 执行语义（新增）
     ├─ t2.5.1 契约 v1.3 定义分派与拦截点                          验收: 冻结
     ├─ t2.5.2 _dispatch_mode 四模式执行落地                      验收: 各自产物落盘
     ├─ t2.5.3 红线接入启动/派工入口                               验收: command:denied 事件
     └─ t2.5.4 单测补齐                                           验收: 全绿

W3 · P2 加固线（与 W2 尾部并行，独立小步可回退）
 ├─ T-3.4 引擎健壮性加固（t3.4.1 events 跨进程锁 / t3.4.2 memory id / t3.4.3 retry 匹配收窄
 │        / t3.4.4 401 码+attempt / t3.4.5 hermes stdin / t3.4.6 汇总定时器+seq 断点 / t3.4.7 step.ok 回写）
 └─ （每 t 独立验收：对应单测绿，全量保持 0 failed）

W4 · P2 验收交付线（W1+W2 收口后，约 1-2 个工作日）
 ├─ T-3.1 e2e 全量干净复跑与归因（复跑/归因/回填）
 ├─ T-3.2 验收清单 Phase2 定稿 + Phase3 清单
 ├─ T-3.3 文档收口（README / 0 字节文档 / INT-04R 拆分 / 规划总览+INT-01 偏差回写 / 部署指南恢复节）
 └─ T-3.5 发布 v0.4.0（tag【推送须用户批准】/ CI e2e-smoke / 发布说明）

并行关系：W1 串行先行；W2 内 T-2.1（契约+前端+适配器）与 T-2.2/2.3（调度+治理）双线并行，
          T-2.5 依赖契约 v1.3，可与 T-2.4 并行；W3 随时可插空；W4 最后。
拓展性预留：全部接线点（scheduler 闸门、ControlBus、审批回流、mode 分派、通知渠道）走事件/策略
          注入而非硬编码，未来新增审批场景、新执行体、新 mode、新通知渠道均为「注册式」扩展。
依赖拍板项（尽早提出）：T-1.2 网关三选一、T-1.4 端口停旧进程、T-1.5 数据处置、T-3.5 push 批准。
```

---

## 3 · 风险与依赖登记

| 风险 | 影响 | 缓解 |
|---|---|---|
| CLI 版本漂移扩散（codex 已加入失败） | T-1.3、T-2.4 | 适配器宽口径 + 条件跳过口径统一 + 失败模式清单；不阻塞 Fake 主链路 |
| 端口 5000 旧进程需用户确认 | T-1.4、e2e 定稿 | 期间用 5050 不阻塞其他线 |
| Hermes 原生 Windows 无官方支持 | T-1.2 选型 | 优先本机 stub，解耦第三方 |
| events 跨进程并发（E1） | W3 前的任何多进程演练 | 尽早落 t3.4.1；演练前避免 watchdog 与手动操作并发写 |
| verify_cmd shell=True 注入面 | Machine Gate | 契约有意设计；红线接线（T-2.5）后双重拦截，文档明示 |
| 契约漂移 | W2 双线并行 | 严格执行「契约 v1.3 冻结后写码」 |
| 同文件并发编辑漏落盘 | 所有编码任务 | 单文件逐次编辑 + 改后 grep 复核 |
| git push 纪律 | T-3.5 | tag/推送前逐一取得用户明确批准（用户既定纪律） |
