# AideanFleet · 剩余任务方案大纲（2026-09-17）

> 编制依据：全量重读 `docs/`（规划总览 / 契约 v1.2 / adr / CLI安装决策单 / 验收清单-Phase2 / 部署指南）
> 与 `.workbuddy/memory/`，并逐项对照**当前真实代码**穿透验证 + 全量测试实跑。
> 本文每条任务均附证据链（文件:行 / 测试实跑结果 / 官方报告），可直接派工执行。

---

## 0 · 分析基线（先看结论）

### 0.1 参考材料的理性与辩证取舍

| 材料 | 结论 | 辩证判定 |
|---|---|---|
| 规划总览（M1/M2 融合决策表） | 分层铁律 Model→Role→Executor→Tools、DAG 是心脏、MockExecutor 先行、Machine Gate 权威 | ✅ 全部已被代码证实落地；其"七坑"清单中 5 坑已填平（见 0.2） |
| M2「前端零构建链，原生 JS」 | 被否决 | ✅ 正确：ADR-016 已裁决 Vue3+Vite+Pinia，核心代码零前端依赖 |
| M2「22 份文档阅读结论」 | 丢弃 | ✅ 材料内容无法验证，只取分析逻辑 |
| 契约 v1「无 WebSocket」 | 已演进 | ✅ ADR-019 + 契约 v1.1/v1.2 补入 WS/session/调度/治理，契约与实现已对齐 |
| CLI 安装决策单（选项 A：FakeAdapter 主验收） | 已定稿 | ⚠️ 与 INT-04 时点探测口径冲突已由经理复核化解（时点演进）；但遗留两个**未兑现后果**：①Manager 网关(9900)启动必败（T-1.2）；②真实 CLI 全链路从未跑通（T-2.4） |
| 验收清单-Phase2 | 仍是「修订中」 | ⚠️ 修订记录已定稿部分条目（202/6/0 基线），但清单整体未宣布出口定稿（T-3.3） |
| .workbuddy 记忆（2026-09-15/16） | 高价值 | ✅ 本轮逐条复验：多数"阻塞"已解除，少数仍真（见 0.2 对照表） |

### 0.2 历史阻塞点 → 当前代码复验对照（证据均为 2026-09-17 实测）

| # | 历史记录的阻塞 | 复验结论 | 证据 |
|---|---|---|---|
| R1 | 两套 BaseAdapter 互不兼容 | **已解决** | `fleet/executors/base.py:18-26` 单向 import `fleet/manager/contracts.py` 契约；5 个真实适配器统一从 `.base` 引入 |
| R2 | `rework_manager.py` 缺失 | **已解决** | 文件存在；`dispatcher.py:164,459` 均引用；`tests/core/test_rework_manager.py` 覆盖 |
| R3 | 无 Scheduler/DAG 调度循环 | **已解决** | `dag.py:106 get_ready_tasks`（READY=ASSIGNED+依赖 DONE+文件域不冲突）、`scheduler.py:159 run_loop`（auto/step 双策略 + ControlBus） |
| R4 | hermes_entry 空行截断、mode 缺 plan | **已解决** | `hermes_entry.py:33` 逐行状态机支持空行；`VALID_MODES` 含 plan（L20,29） |
| R5 | 双 .env 格式不兼容 | **已解决（废弃路线）** | `fleet/console/_deprecated/envstore.py` 已归档；server.py 统一走 `core/config.py` 口径 |
| R6 | notify 未接入引擎事件流 | **已解决** | `launch_core.py:236-244,327` 与 `server.py:1026` 均拉起 `watch_events_loop` |
| R7 | 治理 HTTP 面 404 | **已解决** | `server.py:768-857` 挂载 /api/approvals*、/api/usage/*、/api/budget* |
| R8 | WS chat 与 REST chat 不对称 | **已解决** | `server.py:974-987 _handle_ws_chat` 现调用 `_process_chat` 并落 `chat_reply` + 广播 |
| R9 | ESCALATED 升级链路是否真实 | **已实证** | `data/fleet.db`：Test09161119 有 7 条 ESCALATED 任务（真实跑出 REWORK×3→ESCALATED） |

### 0.3 当前质量基线（实跑）

- 后端全量：`pytest tests/` → **212 passed / 1 failed / 2 skipped**（87s）。唯一失败 = `tests/test_smoke.py::test_opencode_smoke`（真实 CLI 外部依赖漂移，见 T-1.3）。
- e2e：`tests-e2e/e2e-out/.last-run.json` → `status="failed"` 且 `failedTests=[]`（最后一轮为中断式失败，非断言失败；历史各轮好坏参半）。需一次干净全量复跑（T-3.2）。
- git：仅 2 个提交，工作区仅 7 个截图改动；无版本 tag；CI（`.github/workflows/tests.yml`）在位。

---

## 1 · 剩余待完成任务（每条带证据链）

> 判定口径：**P0 = 影响正确性/可信度，必须先修**；**P1 = 打通产品核心价值闭环**；**P2 = 验收与交付收口**。
> 每个原子任务给出：现状证据 → 改动落点 → 验收命令。

### T-1.1 【P0】启动链路健康检查与契约失配（永远判不过）
- 证据链：
  - `fleet/launcher/launch_core.py:154-157`：`_health_check` 判 `data.get("status") == "ok"`；
  - `fleet/console/server.py:270-273`：`/api/health` 契约返回**恰好四键** `version/db/events/config`，**无 status 键**；
  - `docs/契约/控制台API.md` §1：四键契约为 FROZEN；`.workbuddy/memory/2026-09-15.md` 第 27 行早已记录此缺陷，至今未修（本轮复验确认）。
- 后果：CLI/Hermes 引导启动的健康检查永远输出「✗ 控制台健康检查失败」，启动汇总不可信。
- 原子任务：
  - T-1.1.1 改 `_health_check`：四键齐 + `db/events/config` 均真 → console=True（**只改 launch_core，不动契约与 server.py**）；
  - T-1.1.2 补测试：`tests/test_cli_launch.py` 增加"对四键 mock 返回 → True / 缺键 → False"两例（现有用例只测了"返回 dict"）。
- 验收：`pytest tests/test_cli_launch.py -q` 全绿；本地 `python -m fleet.launcher.cli_start`（5050 端口）日志出现「✓ 控制台健康检查通过」。

### T-1.2 【P0】Manager 网关(9900)「假成功」启动
- 证据链：
  - `launch_core.py:119-135 _start_manager_gateway`：`subprocess.Popen(["hermes","-p","manager","gateway","run"])` 后**无条件返回 True**，step 汇总恒 ok；
  - `docs/验收清单-Phase2.md:18`：「A 档决策弃装 Hermes，PATH 口径 unavailable（venv 保留未接入）」→ 本机 PATH 无 hermes，该命令必败；
  - `launch_core.py:167-173`：网关探活打 `/.well-known/agent-card.json` 且要求 `name=="Hermes-Manager"`（同样依赖 hermes 进程真实存在）。
- 后果：启动报告谎报网关已启动；9900 网关能力（规划总览 M1 双入口共享 Application API 的第二入口）实际为零。
- 原子任务：
  - T-2.1（用户拍板项）：三选一——(a) 接入已 clone 的 Hermes venv（决策单留有 venv 未接入）；(b) 用 FastAPI 起一个本机 manager-gateway stub（暴露 agent-card + 转发调度 API），解耦对 hermes CLI 的依赖；(c) 暂时降级：`_start_manager_gateway` 探活失败时 step 如实报 `ok:false + reason`，汇总不谎报。
  - T-2.2 无论选哪条：`_start_manager_gateway` 返回值改为「发命令 → 轮询探活 ≤5s → 以探活结果为准」。
- 验收：`launch_core` 返回的 steps 中 `manager_gateway` 的 ok 与 9900 端口真实探活一致。

### T-1.3 【P0】OpenCode 冒烟测试失败处置（当前全量唯一红）
- 证据链：
  - 本轮实跑：`tests/test_smoke.py::test_opencode_smoke` → `ErrorCode.TOOL_FAIL`（CLI stderr 非 JSON 输出，本机 opencode 1.18.31 行为漂移）；
  - `docs/CLI安装决策单.md`：CLI 版本"时点演进"是既认风险（决策单·剩余风险条目）。
- 原子任务：
  - T-3.1 复跑定位：单独跑该用例抓 stderr，区分「CLI 升级导致输出格式漂移」vs「适配器解析缺陷」（`fleet/executors/opencode.py`）；
  - T-3.2 按结论二选一：适配器输出解析兼容升级后的 CLI（优先），或将该冒烟用例标记 `@pytest.mark.external_cli + skipif`（与 hermes 6 个条件跳过同口径），保证全量基线回到绿。
- 验收：`pytest tests/ -q` → 0 failed。

### T-1.4 【P0】端口 5000 归位（ADR-017 定版落地，需用户拍板）
- 证据链：
  - `.workbuddy/memory/MEMORY.md:4`：5000 被旧版控制台 PID 34196（`version=3.1.0-launchbus`）占用；
  - `docs/adr.md` ADR-017：UI=控制台=5000 为定版；契约 §0 同。
- 原子任务：T-4.1 向用户确认后停旧进程 → T-4.2 `FLEET_CONSOLE_PORT=5000` 起新控制台 → T-4.3 e2e 默认 baseURL 切回 5000（`tests-e2e/playwright.config.ts` + `E2E_BASE_URL`）→ T-4.4 删除 `reports/ui03r/switch-to-5000.sh` 这类临时切换脚本的依赖说明。
- 验收：`curl http://127.0.0.1:5000/api/health` 返回本包 version；e2e 全量在 5000 上跑通。

### T-2.1 【P1】stream_chunk 生产方落地（契约预留唯一未兑现项）
- 证据链：
  - `docs/契约/控制台API.md:539`（§8）：`stream_chunk` 标注「**预留**：执行体流式输出（INT-03 接入），当前无产生方」；
  - 本轮 grep：`stream_chunk` 在 `fleet/**/*.py` **零命中**（生产侧确无实现）；
  - 前端已按契约实现并有注入验证通道 `window.__fleetInjectStream`（`reports/UI-03R-evidence.md` + memory 记录）。
- 原子任务：
  - T-5.1 契约先修（v1.3 草案）：定义 chunk 粒度（行/缓冲阈值）、`taskId+chunk+seq` 字段、与 `chat_reply` 的关系——避免并行开发字段漂移（呼应规划总览「契约先行铁律」）；
  - T-5.2 执行体侧：`BaseAdapter` 增加 `stream=True` 可选产出（先做 FakeAdapter 流式，符合「Mock 先行」铁律）；
  - T-5.3 server 侧：WS 广播 `stream_chunk`，REST 侧缓冲拼装后落 `chat_reply`（保持两通道对称，防 UI-03R 已修的不对称回归）；
  - T-5.4 e2e：把 `stream.spec.ts` 从「注入验证」升级为「真实后端帧验证」。
- 验收：真实（先 Fake）执行期间对话页出现逐帧增量气泡且来源标注正确；`pytest tests/core/test_server_api.py` 新增用例绿。

### T-2.2 【P1】三层预算熔断接入调度闭环（治理层与发动机未接线的最后一公里）
- 证据链：
  - `fleet/governance/budget.py`：`BudgetManager.check/consume` + `is_exceeded` 熔断器**已实现且 28 例测试绿**；
  - `fleet/manager/scheduler.py` / `dispatcher.py`：grep 证实**零处** import `governance`——派工前不查预算、超限不暂停；
  - `fleet/manager/context_budget.py` 模块头自述：「本模块不做任何熔断决策」（其 budget:check 仅限 prompt 组装口径）；
  - `server.py:748`：BudgetManager 仅作为 HTTP `/api/budget` 只读消费；契约 v1.2 §13「熔断策略归角色D」——即**熔断决策目前无人认领**。
- 原子任务：
  - T-6.1 契约 v1.3 补一节：`budget:exceeded` 之后 run_loop 的行为定义（pause 派工 + 发通知 + 等审批，复用 approval.py 的 `budget_warning` 场景）；
  - T-6.2 `scheduler.schedule_once` 派工前插 `BudgetManager.check(project_id)`：`is_exceeded` → 跳过派工 + `events.append(budget:exceeded)` + 建审批单；
  - T-6.3 `dispatcher` 回执处理处插 `consume(usage.total_tokens)`（数据已在 `model:call` 事件里，`dispatcher.py:207`）；
  - T-6.4 审批通过 → `_sync_events_budget_after_decision`（server.py:808 已有）→ run_loop 下一拍恢复。补 e2e：超限→审批→恢复全链路。
- 验收：单测模拟消费超限后 `schedule_once` 不再派工且事件流出现 `budget:exceeded` + 审批单；批准后恢复派工。

### T-2.3 【P1】审批门（approval gate）接入关键动作
- 证据链：`governance/approval.py`（三种场景+超时自动拒绝，测试绿）与 `/api/approvals*` 路由（server.py:768-820）均**只服务前端展示**；派工/启动路径无任何审批消费点。
- 原子任务：
  - T-7.1 定义"需审批动作"清单（建议首版两项：预算超限放行【与 T-2.2 共用】、step 模式下 BLOCKED 任务人工重派）；
  - T-7.2 scheduler 在上述动作点创建审批单并挂起（ControlBus 增加 approval 维度）；
  - T-7.3 审批决定事件回流 run_loop。
- 验收：审批中心页面点「通过」后，被挂起的调度动作真实继续（页面统计联动已有 e2e 用例，需加服务端联通用例）。

### T-2.4 【P1】真实 CLI 全链路首跑（从 FakeAdapter 验收走向真实价值）
- 证据链：
  - `data/fleet.db`：仅有 Test09161119 / TestTable0916 的测试任务；**AideanFleet 项目自身 0 任务**（`data/projects/AideanFleet/plan.json` → `"阶段": []`）；
  - `docs/CLI安装决策单.md` 选项 A：「真实执行以 FakeAdapter 为主」——即"intake→派工→真实 CLI 执行→Machine Gate→报告→通知"全链路从未用真实 CLI 走通；
  - Claude Code / Codex / OpenCode 本机均在位（决策单实测表）。
- 原子任务：
  - T-8.1 选最小真实项目（如本仓库的一个文档级小任务）走 `/api/intake` → `schedule_once`；
  - T-8.2 验证 dispatcher → claudecode 适配器 → `gates/verify.py` 四道门 → 六节报告落 `evidence/` → 事件流闭环；
  - T-8.3 记录首个真实任务的 `reports/` 证据（事件 JSONL + 报告 + 截图），作为 Phase 3 验收锚点；
  - T-8.4 沉淀失败模式清单（CLI 输出漂移/超时/树杀进程）回填 `models/retry.py` 口径验证。
- 验收：`data/fleet.db` 出现 AideanFleet 项目一条 DONE 任务且带真实 evidence 目录。

### T-3.1 【P2】e2e 全量干净复跑 + 定稿
- 证据链：`tests-e2e/e2e-out/.last-run.json` = failed/空 failedTests（中断）；历史轮次 41 绿为 UI-03R 时点结论。
- 原子任务：T-9.1 修完 T-1.x 后全量复跑（保持 trace/screenshot/video off + 唯一 outputDir，防 safe-delete 护栏假失败）→ T-9.2 失败用例逐条归因（真缺陷/环境/断言脆弱）→ T-9.3 结果写入验收清单。
- 验收：一轮全绿或每条红均有归因记录。

### T-3.2 【P2】验收清单-Phase2 定稿 + Phase 3 清单编制
- 证据链：`docs/验收清单-Phase2.md:3-6` 仍标「修订中」；修订记录已定稿 202/6/0 基线但清单正文未同步出口结论。
- 原子任务：T-10.1 把修订记录结论回填正文各表 → T-10.2 新增本文 T-1~T-3 的出口条目形成 Phase 3 验收清单 → T-10.3 经理复核签字定稿。
- 验收：清单无「修订中」字样；Phase 3 清单每条可执行、可测。

### T-3.3 【P2】文档收口
- 证据链：`README.md` 仅 1 行标题；`docs/启动Hermes派工3(英文版).txt`、`启动Hermes派工4(中文版).txt` 均 **0 字节**；`reports/INT-04R-evidence.md` 缺失（验收清单 L19 自认，内容内嵌于 INT-04-evidence.md）；`docs/规划总览.md` 头部还残留"核对分析过程"草稿性内容。
- 原子任务：
  - T-11.1 README：一段定位 + 快速启动（引用部署指南）+ 测试命令 + 目录导览；
  - T-11.2 两份 0 字节派工文档：补全或删除（补全内容以派工3中文版为蓝本）；
  - T-11.3 抽出 `reports/INT-04R-evidence.md`（从内嵌内容拆分，兑现验收清单承诺）；
  - T-11.4 规划总览的「已完成/未完成」状态段刷新为本大纲 §0.2/§1 结论（防止下次会话基于过期状态派工）。
- 验收：`wc -c docs/*.txt` 无 0 字节；README 可让新人 10 分钟跑起来。

### T-3.4 【P2】版本化与发布工程
- 证据链：git 仅 `first commit` + `0c94fc7`；无 tag；部署指南自述版本 0.3.0；CI 在位（tests.yml，含失败日志构件）。
- 原子任务：T-12.1 全绿基线后打 tag `v0.4.0`（内容=T-1 修复+T-2 闭环）→ T-12.2 CI 补 e2e job（可先 only- smoke spec，全量留在本地）→ T-12.3 CHANGELOG 或发布说明（复用六节报告模板口径）。
- 验收：`git tag` 存在 v0.4.0；CI 在 GitHub 上绿。

---

## 2 · 执行方案大纲（多层级 · 最小原子 · 可执行）

```markdown
AideanFleet 收口计划（三层结构：W 工作流 → T 任务 → t 原子步骤）

W1 · P0 正确性修复线（约 1 个工作日，串行即可）
 ├─ T-1.1 启动健康检查契约失配
 │   ├─ t1.1.1 修 launch_core._health_check 判定（四键口径，只动 launcher）
 │   ├─ t1.1.2 tests/test_cli_launch.py 补 2 例（四键 mock 真/假）
 │   └─ t1.1.3 实跑 cli_start@5050 验证日志通过
 ├─ T-1.2 Manager 网关假成功
 │   ├─ t1.2.1 【用户拍板】接入 hermes venv / 本机 stub / 如实降级 三选一
 │   ├─ t1.2.2 _start_manager_gateway 改为探活后置（≤5s 轮询定 ok）
 │   └─ t1.2.3 steps 汇总与 9900 实况一致性验证
 ├─ T-1.3 OpenCode 冒烟红
 │   ├─ t1.3.1 单跑抓 stderr，归因（CLI 漂移 vs 解析缺陷）
 │   ├─ t1.3.2 适配器兼容修复（优先）或 @external_cli 条件跳过
 │   └─ t1.3.3 全量 pytest 回到 0 failed
 └─ T-1.4 端口 5000 归位【依赖用户确认】
     ├─ t1.4.1 确认并停旧 PID 34196
     ├─ t1.4.2 5000 起新控制台 + health 四键验证
     └─ t1.4.3 e2e baseURL 切回 5000，清理临时切换脚本依赖

W2 · P1 核心价值闭环线（约 2-3 个工作日；W1 完成后启动，T-2.1 与 T-2.2/2.3 可并行）
 ├─ T-2.1 stream_chunk 生产方（契约 v1.3 先行）
 │   ├─ t2.1.1 契约 v1.3 草案：chunk 字段/粒度/与 chat_reply 关系（冻结后再写码）
 │   ├─ t2.1.2 BaseAdapter 可选流式接口 + FakeAdapter 流式实现（Mock 先行）
 │   ├─ t2.1.3 server WS 广播 stream_chunk + REST 缓冲对称落 chat_reply
 │   └─ t2.1.4 stream.spec.ts 升级为真实后端帧验证
 ├─ T-2.2 三层预算熔断接入调度
 │   ├─ t2.2.1 契约 v1.3 补 §14：budget:exceeded 后 run_loop 行为定义
 │   ├─ t2.2.2 schedule_once 派工前 BudgetManager.check 闸门 + 审批单创建
 │   ├─ t2.2.3 dispatcher 回执处 consume(model:call.usage)（数据已在事件中）
 │   └─ t2.2.4 超限→审批→恢复 全链路测试（单测+e2e 各一）
 ├─ T-2.3 审批门接入关键动作
 │   ├─ t2.3.1 需审批动作清单定版（首版 2 项）
 │   ├─ t2.3.2 ControlBus 增加 approval 挂起维度
 │   └─ t2.3.3 决定事件回流 run_loop + 审批中心服务端联通用例
 └─ T-2.4 真实 CLI 全链路首跑
     ├─ t2.4.1 选最小真实任务 intake
     ├─ t2.4.2 claudecode 派工→四道门→报告→事件闭环实测
     ├─ t2.4.3 reports/ 首个真实任务证据落盘
     └─ t2.4.4 失败模式清单回填 retry 口径验证

W3 · P2 验收交付线（约 1-2 个工作日；W2 收口后）
 ├─ T-3.1 e2e 全量干净复跑与归因（含 t3.1.1 复跑 / t3.1.2 归因 / t3.1.3 清单回填）
 ├─ T-3.2 验收清单 Phase2 定稿 + Phase3 清单编制
 ├─ T-3.3 文档收口（README / 两份 0 字节文档 / INT-04R-evidence 拆分 / 规划总览状态刷新）
 └─ T-3.4 发布 v0.4.0（tag + CI e2e-smoke job + 发布说明）

并行关系：W1 内部串行；W2 的 T-2.1（前端+契约）与 T-2.2（后端调度）可双线并行；
          T-1.4 与 T-2.4 都依赖用户拍板，应尽早提出。
拓展性预留：W2 全部接线点（scheduler 闸门、ControlBus、审批回流）走事件/策略注入而非硬编码，
          未来新增审批场景、新执行体、新通知渠道均为"注册式"扩展，不动 run_loop 主干。
```

---

## 3 · 风险与依赖登记

| 风险 | 影响 | 缓解 |
|---|---|---|
| 端口 5000 旧进程停用需用户确认 | W1/T-1.4、e2e 定稿 | 第一时间提请拍板，期间用 5050 不阻塞其他线 |
| Hermes 原生 Windows 无官方支持 | T-1.2 选型 | 优先本机 stub 方案，解耦对第三方 CLI 的依赖 |
| CLI 版本时点演进（决策单已认） | T-1.3、T-2.4 | 冒烟用例条件化 + 适配器解析宽口径 + 失败模式清单 |
| safe-delete 护栏批量删除拦截 | e2e 复跑 | 维持 trace/screenshot/video off + 唯一 outputDir（memory 已验证对策） |
| 同文件并发编辑漏落盘 | 所有编码任务 | 单文件逐次编辑 + 改后 grep 复核（历史两次踩坑） |
| 契约漂移 | W2 双线并行 | 严格执行"契约 v1.3 冻结后写码"顺序 |
```
