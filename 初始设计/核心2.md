"""
# 笔记整理
## .env.example
- 基础信息
- 模型池
- 角色模型及启动方式


## 项目结构

### .env.example
模板。禁止写入

### .env
初始化时从.env.example复制，可读可写，实时读取无缓存。

### 数据库
> 任务进度表
- 任务id、角色、下发角色，回执角色、平台、模型、任务类型、token、耗时、详情、备注、执行状态、审查状态、创建时间、更新时间。
- 文本型数据库：存任务分配、存执行过程、存审查结果
- 字段-> 任务类型：(1=分配,2=执行,3=审查)
- 字段-> 执行状态：(1=等待,2=执行中,3=执行完成)
- 字段-> 审查状态：(1=等待,2=审查中,3=审查通过，4=审查不通过)

### plan.json
- 三级大纲的规划目录，每次manager分配后，实时同步更新，每个阶段任务通过<任务id>对应。

## 启动流程
- 方式一：cli引导启动 -> hermes接收指令
- 方式二：hermes直接接收指令
无论哪种方式，首先打开网页控制端，由网页动态控制。

### 网页控制端设计
深色科技风格
锁屏：输入项目名解锁（.env默认1小时有效期）；
顶部栏-右上角：锁定键+设置按钮（点击展开设置表单）；
无底部信息栏；
最左侧：菜单栏 可缩起可展开；一级大纲顺序为：[总览,对话,模型,角色,执行体,拓展,消息]
最右侧：进度条，可缩起可展开，默认缩起。从顶部栏到底。缩起状态下呈空心圆型，完成的呈绿色，未完成的呈红色；当前显示的位置呈青碧色（圆要比其他的大些，中间写百分比进度条）；
左右侧的菜单栏/进度条均默认显示第一级大纲，用户自行点击展开二三级大纲（没有就算了）；可以左右拉伸(拖动)边缘线调整宽度；

- 1. 总览（右侧显示"每步确认"/"自动执行"的切换按钮，如果是每步确认，那么最下方会有输入框，用户可以交流说出自己的意见，也可以确认）
总览呈现的是信息面板，呈现的主体永远是当前正在执行的步骤，已执行的将会折叠到顶部，点击展开可以查看过程。
"""
动态实时信息面板，支持鼠标中间上下滑动查看进度。
显示执行前的固定步骤，例如：
- 正在初始化...
- 正在准备...1.接收指令 (1/10) 
- 正在准备...2.分析需求 (2/10)
- 正在准备...3.初始化Manager (3/10)  
- 正在准备...4.建立项目主体计划 (4/10)  
- 正在准备...5.确认创建模型库 (5/10)  
- 正在准备...6.确认创建角色库 (6/10)
- 正在准备...7.确认创建执行体 (7/10)
- 正在准备...8.确认创建工具库 (8/10)  
- 正在准备...9.确认创建提示词 (9/10) 
- 一切就绪!是否启动?(10/10) 
"""

- 2. 对话
只要程序主体还在执行，用户可以通过对话和Manager交流，实时调整/修正任务信息。

- 3. 模型
"""
name = "agnes3"
level = 5 //优先级
base_url = "https://apihub.agnes-ai.com/v1" //请求地址
model_id = "agnes-3.0-flash" //模型id
api_key = "***REMOVED***MQi8upquTH5uDvqeRc22v9G5YHu083qNFPdFHLPzB" //密钥
http_proxy = "" //默认为空，但有些比如B.AI需要走本地代理10808端口才能访问
https_proxy = "" //默认为空，但有些比如B.AI需要走本地代理10808端口才能访问
"""

- 4. 角色
.env.example默认=1个产品+UI设计，1个前端，1个数据交互，2个后端，1个审查。
"""
role_name = ```角色名```
system_prompt = ```系统设定提示词```
bind_model_name = [agnes3,agnes2,agnes1] //此为示例，第一个不通切第二个
"""

- 5. 执行体
执行任务的智能体，比如claudecode、opencode、codex、hermes子agent等等
可配置启动命令，默认启动命令=.env是初始化时，从.env.example复制来的

- 6. 拓展
配置执行体可使用的skills和mcp

- 7. 消息
配置邮箱提醒的参数，从.env中读取(.env是初始化时，从.env.example复制来的),默认发送到"164093410@qq.com"
```
import smtplib
from email.mime.text import MIMEText
from email.mime.multipart import MIMEMultipart
sender_email = "***REMOVED***@163.com"
receiver_email = "164093410@qq.com"
password = "***REMOVED***"
message = MIMEText("测试内容", _subtype='plain', _charset='utf-8')
message['From'] = sender_email  # 邮件的发送者
message['To'] = receiver_email  # 邮件的接收者
message['Subject'] = Header(f'测试推送', 'utf-8')  # 邮件的标题
smtper = smtplib.SMTP_SSL("smtp.163.com", 465)
smtper.login("***REMOVED***@163.com", "***REMOVED***")
smtper.sendmail(sender_email, receiver_email, message.as_string())
```
配置什么时候消息提醒，勾选，触发后发一条。(1)任务开始时，默认关闭;(2)任务结束时，默认开启;(3)Manager模型额度不够时，默认开启;(4)执行角色模型额度不够时，默认开启;
其他情况你来拓展。


### 设置表单
1. 设置请求超时时间
2. 设置请求重试次数
其他你来拓展
"""