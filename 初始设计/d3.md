
可以。你这次的问题，我建议不要再停留在“Cursor + Claude Code + Codex + WorkBuddy + OpenCode 怎么串起来”这个层面，而是把它当成一个**AI Autonomous Software Factory（自主软件工厂）**来研究。

我会先给结论，再把**开源项目、论文、当前 Agent 架构、协议、实际工具能力、自动化闭环、可行性边界**逐层拆开。

# 一、结论先行

你的思路**技术上是可行的**，而且目前已经出现了足够多的技术组件，可以把它实现到一个相当高的自动化程度。

但是，我建议把你的原始架构：

```text
Cursor
  ↓
Claude Code
  ↓
Codex
  ↓
WorkBuddy
  ↓
OpenCode
```

彻底升级成：

```text
                    AI Software Factory
                            │
                            ▼
                  ┌──────────────────┐
                  │   Orchestrator   │
                  │   自动调度中枢    │
                  └────────┬─────────┘
                           │
                  Technical Manager
                           │
          ┌────────────────┼────────────────┐
          │                │                │
          ▼                ▼                ▼
       UI Agent       Frontend Agent    Backend Agent
      Claude Code        Codex           OpenCode
          │                │                │
          └────────────┬───┴────────────────┘
                       │
                       ▼
                  Data/API Agent
                    WorkBuddy
                       │
                       ▼
                 QA / Review Agent
                       │
                 ┌─────┴─────┐
                 │           │
                PASS        FAIL
                 │           │
                 ▼           ▼
               Merge       Rework
                             │
                             ▼
                         Re-test
                             │
                             └───────→ Loop
```

**真正的核心不是“5 个 AI 工具”。**

而是：

> **一个可持久运行的任务状态机 + DAG 调度器 + 多 Agent Worker + Git/CI 验收体系 + 自动返工闭环。**

这点在学术研究和目前开源实践里都有非常明确的依据。

MetaGPT 直接把“产品经理、架构师、项目经理、工程师”抽象成一个软件公司，并用 SOP（Standard Operating Procedure）把软件开发流程编码进去。([GitHub][1])

ChatDev 则从“多角色 Agent 通过结构化通信完成设计、编码、测试”出发。([arXiv][2])

而更新一代的 OpenHands 已经开始把这种思想进一步工程化成 **Agent Server + SDK + Automation + 多 Agent 后端**。([GitHub][3])

所以，你想做的东西不是一个异想天开的方案。

实际上：

> **你是在把过去 MetaGPT / ChatDev 的“AI 软件公司”思想，与现在 Cursor / Codex / Claude Code / OpenCode 这些真正能修改代码的 Coding Agent 结合起来。**

这比单纯使用 MetaGPT 更先进。

---

# 二、为什么你原来的“AI 员工”思路是正确的

你定义：

| 人类公司角色    | AI角色        |
| --------- | ----------- |
| 产品技术经理    | Cursor      |
| UI设计师     | Claude Code |
| 前端工程师     | Codex       |
| 数据/API工程师 | WorkBuddy   |
| 后端工程师     | OpenCode    |

这个组织结构本身非常合理。

因为现在的 Coding Agent 已经不再只是“代码补全”。

例如 Cursor 当前已经支持多个 Agent 并行工作，并且 Cloud Agent 可以独立运行、修改代码、执行测试并创建 PR。([Cursor][4])

Codex 也已经把产品形态推进到了：

> 同时管理多个 Agent、并行工作、长期任务、完整软件生命周期。

OpenAI 官方在 2026 年的 Codex App 介绍中已经明确把它定位成可以管理协调 Agent 团队的开发环境。([OpenAI][5])

Claude Code 也已经支持 Agent Teams：

```text
Team Lead
   │
   ├── Worker A
   ├── Worker B
   └── Worker C
```

具有：

* 独立 Context
* Shared Task List
* Agent Messaging
* Task Dependencies
* Parallel Execution

这些能力。([Claude][6])

所以：

**“AI 软件公司”这个抽象已经从论文逐渐变成实际产品能力。**

---

# 三、但真正难的不是“Agent 会写代码”

这是整个研究最重要的结论之一。

真正困难的是：

```text
需求
 ↓
任务
 ↓
依赖
 ↓
执行
 ↓
交付
 ↓
验证
 ↓
发现问题
 ↓
重新派工
 ↓
再次验证
 ↓
最终完成
```

也就是说：

# 难点是 Workflow，而不是 Agent。

这和 MetaGPT 的研究结论高度一致。

MetaGPT 的核心思想就是：

> **Code = SOP(Team)**

也就是把人类软件公司的标准流程编码成 Agent 工作流，而不是简单地让多个 Agent 自由聊天。([arXiv][7])

这对你的项目非常关键。

---

# 四、因此第一原则：不要做“Agent Chat”

千万不要做：

```text
Cursor：

“Claude，你帮我做UI。”

Claude：

“好的。”

Claude：

“Codex，你可以开始了。”

Codex：

“好的。”

Codex：

“OpenCode，你帮我写API。”

……
```

这是最容易失控的模式。

因为：

```text
上下文不一致
+
需求漂移
+
接口漂移
+
责任不清
+
重复工作
+
无法恢复
```

最终就会变成：

> **5 个 AI 一起改项目。**

而不是：

> **5 个 AI 组成软件公司。**

---

# 五、应该采用“Agent + Workflow”架构

正确模型应该是：

```text
                 Project
                    │
                    ▼
               Orchestrator
                    │
              Project State
                    │
              Task DAG
                    │
       ┌────────────┼────────────┐
       ▼            ▼            ▼
     Task A       Task B       Task C
       │            │            │
       ▼            ▼            ▼
     Agent        Agent        Agent
       │            │            │
       └────────────┼────────────┘
                    │
                    ▼
                 Review
                    │
              ┌─────┴─────┐
              ▼           ▼
             PASS        FAIL
              │           │
              ▼           ▼
            Merge       Rework
```

这也是我认为你应该重点借鉴 **Mastra / LangGraph / Microsoft Agent Framework / OpenHands SDK** 的地方。

Mastra Workflow 本身就是为“复杂、多步骤、可重复、可审计”的 Agent 流程设计的。([Mastra][8])

---

# 六、开源世界已经出现了几条不同路线

我把目前值得研究的项目分成五类。

## 第一类：AI Software Company

### MetaGPT

[MetaGPT GitHub](https://github.com/FoundationAgents/MetaGPT?utm_source=chatgpt.com)

这是与你的想法最接近的项目之一。

它直接模拟：

```text
Product Manager
      ↓
Architect
      ↓
Project Manager
      ↓
Engineer
```

并通过 SOP 控制协作。([GitHub][1])

论文也明确指出，它针对多 Agent 简单串联容易出现的逻辑不一致、级联幻觉等问题，引入标准化软件工程流程。([arXiv][7])

### 对你最大的价值

不是直接拿来当底座。

而是学习：

```text
Role
SOP
Artifact
Dependency
Review
```

这个思想。

---

# 七、第二类：ChatDev

[ChatDev GitHub](https://github.com/OpenBMB/ChatDev?utm_source=chatgpt.com)

论文：

**ChatDev: Communicative Agents for Software Development**

它把软件开发拆成：

```text
Design
 ↓
Coding
 ↓
Testing
```

然后让不同角色 Agent 进行结构化通信。([arXiv][2])

它特别值得你学习：

> **Agent Communication Protocol**

也就是：

```text
Agent A
  ↓
Structured Message
  ↓
Agent B
```

而不是自然语言闲聊。

不过它的思想偏研究型，作为你最终生产系统的底座，我不会优先推荐。

---

# 八、第三类：OpenHands

这个项目我认为你必须重点研究。

[OpenHands GitHub](https://github.com/OpenHands/OpenHands?utm_source=chatgpt.com)

它现在已经不只是一个 Coding Agent。

它提供：

```text
Agent SDK
Agent Server
Automation
Agent Canvas
Remote Workspace
Docker / Kubernetes
GitHub Workflows
```

并且 Agent Canvas 可以调度：

```text
OpenHands
Claude Code
Codex
Gemini
ACP-compatible agents
```

官方当前的定位已经非常接近：

> **Self-hosted engineering team / developer control center**

([GitHub][3])

这是一个非常重要的变化。

---

# 九、OpenHands 对你的价值甚至可能比 MetaGPT 更大

因为 MetaGPT 更像：

```text
AI 软件公司模拟器
```

而 OpenHands 更像：

```text
AI Software Agent Runtime
```

你真正需要的是后者。

因为你已经有：

```text
Claude Code
Codex
OpenCode
WorkBuddy
Cursor
```

你不需要重新训练一群 Agent。

你需要的是：

> **把这些 Agent 变成可以被统一调度的 Worker。**

OpenHands SDK 就是值得研究的这层。

它支持：

* Python
* TypeScript
* REST
* Agent Server
* Docker
* Kubernetes
* Remote Workspace
* 多 Agent
* GitHub Workflow

([GitHub][9])

---

# 十、第四类：SWE-agent

[SWE-agent GitHub](https://github.com/SWE-agent/SWE-agent?utm_source=chatgpt.com)

这个项目的价值不在“软件公司”。

而在：

# 如何让 Agent 真正操作代码库。

SWE-agent 提出了非常重要的：

> Agent-Computer Interface（ACI）

即：

```text
LLM
 ↓
ACI
 ↓
Repository
 ↓
Tests
 ↓
Feedback
```

而不是让 LLM 直接面对巨大代码库。([GitHub][10])

这对你的系统非常重要。

因为你的每个 Worker 都应该有明确的：

```text
Workspace
Tools
Permissions
Context
Commands
Tests
```

而不是无限制访问整个项目。

---

# 十一、第五类：Microsoft Agent Framework

这里需要特别注意一个变化。

过去大家经常推荐：

```text
AutoGen
```

但是现在 AutoGen 官方仓库已经进入 **Maintenance Mode**，微软明确建议新项目使用：

> **Microsoft Agent Framework**

([GitHub][11])

Microsoft Agent Framework 当前强调：

```text
Sequential
Concurrent
Handoff
Group Collaboration
Multi-agent
MCP
A2A
```

并且定位就是 production-grade multi-agent workflows。([GitHub][12])

所以如果从 2026 年的新项目角度研究：

```text
AutoGen
```

应该作为**历史参考架构**。

而不是你的首选底座。

---

# 十二、论文方面，最值得借鉴的是三个思想

## 1. MetaGPT：SOP

核心：

```text
Agent ≠ 自由发挥

Agent = Role + SOP + Artifact + Review
```

([arXiv][7])

---

## 2. ChatDev：Communication

核心：

```text
Agent
 ↓
Structured Communication
 ↓
Agent
```

([arXiv][2])

---

## 3. AgentCoder：测试驱动闭环

这个对你的“自动返工”尤其重要。

AgentCoder 把系统拆成：

```text
Programmer Agent
       ↓
Test Designer Agent
       ↓
Test Executor Agent
       ↓
Feedback
       ↓
Programmer
```

也就是：

```text
写代码
 ↓
写测试
 ↓
执行测试
 ↓
反馈
 ↓
修复
 ↓
再测试
```

论文实验显示，这种多 Agent + 测试反馈架构在其测试集上显著优于单一代码生成方式。([arXiv][13])

这实际上与你要求的：

> “完成阶段性任务 → 技术经理检查 → 发现问题 → 安排修复 → 直到完成”

高度一致。

---

# 十三、因此，你的系统应该是“四层闭环”

我建议最终设计成：

```text
                    ┌─────────────────┐
                    │   Project Spec  │
                    └────────┬────────┘
                             │
                             ▼
┌────────────────────────────────────────────────┐
│                 Planning Layer                 │
│                                                │
│ Product Manager                                │
│ Architect                                      │
│ Technical Manager                              │
│ Task Planner                                   │
└───────────────────────┬────────────────────────┘
                        │
                        ▼
┌────────────────────────────────────────────────┐
│                  Execution Layer                │
│                                                │
│ UI Agent                                       │
│ Frontend Agent                                 │
│ Data Agent                                     │
│ Backend Agent                                  │
└───────────────────────┬────────────────────────┘
                        │
                        ▼
┌────────────────────────────────────────────────┐
│                 Verification Layer             │
│                                                │
│ Unit Test                                       │
│ Integration Test                                │
│ E2E Test                                        │
│ Visual Test                                     │
│ Security Test                                   │
│ Architecture Review                             │
└───────────────────────┬────────────────────────┘
                        │
                        ▼
┌────────────────────────────────────────────────┐
│                  Recovery Layer                 │
│                                                │
│ Failure Analysis                               │
│ Bug Classification                             │
│ Task Reassignment                               │
│ Retry                                           │
│ Rollback                                        │
│ Re-test                                         │
└───────────────────────┬────────────────────────┘
                        │
                        └──────────→ Execution
```

这四层缺一不可。

---

# 十四、真正的核心：Task DAG

你的技术经理不能简单地：

```text
任务1
任务2
任务3
任务4
```

必须生成：

# Dependency DAG

例如：

```text
                   Project
                      │
                      ▼
               Architecture
                      │
          ┌───────────┼───────────┐
          │           │           │
          ▼           ▼           ▼
        UI Design   DB Schema   API Contract
          │           │           │
          │           ▼           ▼
          │        Backend API    Data Layer
          │           │           │
          └───────────┼───────────┘
                      │
                      ▼
                  Frontend
                      │
                      ▼
                 Integration
                      │
                      ▼
                     E2E
                      │
                      ▼
                     QA
```

其中：

```text
UI Design
DB Schema
API Contract
```

可以并行。

而：

```text
Frontend
```

必须等待：

```text
UI Design
+
API Contract
```

---

# 十五、这样才能真正实现你的第 6 条要求

你的原始要求：

> UI、前端、数据、后端可以并行。

我建议稍微修改成：

> **所有无依赖任务自动并行；存在依赖的任务自动等待；依赖完成后自动解除阻塞。**

这比“固定四个 Agent 并行”高级很多。

---

# 十六、必须建立“项目事实源”

这是我认为你整个方案最重要的工程设计。

不要让 Agent 之间共享自然语言上下文。

建立：

```text
.ai/
```

例如：

```text
.ai/
├── project.yaml
├── state.yaml
├── architecture/
├── contracts/
├── tasks/
├── decisions/
├── reviews/
├── failures/
├── reports/
└── agents/
```

---

# 十七、每一个任务必须有标准协议

例如：

```yaml
id: FE-USER-001

role: frontend

status: READY

priority: P1

depends_on:
  - UI-USER-001
  - API-AUTH-001

inputs:
  - docs/ui/user-login.md
  - docs/api/auth.openapi.yaml

outputs:
  - src/pages/login
  - tests/login

acceptance:
  - lint
  - typecheck
  - unit-test
  - e2e

review:
  required: true

max_retries: 5
```

Agent 不能说：

> “我觉得完成了。”

只能：

```text
SUBMIT
```

---

# 十八、提交以后进入验收状态

完整状态机应该是：

```text
CREATED
   ↓
PLANNED
   ↓
READY
   ↓
ASSIGNED
   ↓
RUNNING
   ↓
SUBMITTED
   ↓
TESTING
   ↓
REVIEWING
   ↓
┌──┴──┐
│     │
PASS FAIL
│     │
│     ▼
│   REWORK
│     │
│     ▼
│   RUNNING
│
▼
COMPLETED
```

这就是你要求的：

> “直到该员工的阶段性任务全部完成。”

---

# 十九、失败不能只返回“失败”

必须生成：

```yaml
failure:
  type: API_CONTRACT_MISMATCH

severity: P1

owner: backend

evidence:
  - test/auth-login.spec.ts
  - api-response.json

expected:
  status: 401

actual:
  status: 500

suspected_files:
  - src/auth/auth.service.ts

next_action:
  - fix_backend
```

然后 Orchestrator 自动：

```text
Backend Agent
```

重新接任务。

---

# 二十、这里可以引入一个非常重要的概念：Artifact

每个 Agent 不是只提交代码。

而是提交：

```text
代码
+
测试
+
报告
+
证据
```

例如 UI Agent：

```text
UI
├── implementation
├── screenshots
├── states
└── design-report
```

Backend：

```text
Backend
├── code
├── tests
├── openapi
└── implementation-report
```

Frontend：

```text
Frontend
├── code
├── tests
├── screenshots
└── implementation-report
```

这样技术经理才有办法自动 Review。

---

# 二十一、UI Agent 尤其需要“视觉验收”

这是你的流程里容易被忽略的地方。

UI：

```text
Claude Code
     ↓
生成页面
     ↓
启动项目
     ↓
Playwright
     ↓
Screenshot
     ↓
Vision Reviewer
     ↓
PASS / FAIL
```

而不是：

```text
Claude：
“页面已经完成。”
```

---

# 二十二、前端和后端之间不要直接“猜接口”

必须：

# Contract First

例如：

```text
Backend
   ↓
OpenAPI
   ↓
Contract Review
   ↓
Frontend
```

然后：

```text
OpenAPI
   ↓
Generated Types
   ↓
Generated Client
```

这样可以极大降低：

```text
接口名称不一致
字段不一致
类型不一致
状态码不一致
分页方式不一致
```

---

# 二十三、MCP 和 ACP 在这里非常重要

你想把：

```text
Cursor
Claude Code
Codex
OpenCode
WorkBuddy
```

放进同一个体系。

这里有两个协议值得重点研究：

## MCP

MCP 解决：

> Agent ↔ Tool / Data

例如：

```text
Agent
 ↓
MCP
 ├── GitHub
 ├── Database
 ├── Browser
 ├── Jira
 ├── Filesystem
 └── CI
```

MCP 官方规范就是为 LLM 提供标准化工具访问能力。([Model Context Protocol][14])

---

# 二十四、ACP 更接近你要解决的问题

ACP：

> Agent Client Protocol

它解决：

```text
Editor
   ↕
Agent
```

即让不同编辑器和 Coding Agent 通过统一协议互操作。([GitHub][15])

现在 ACP 的 Agent 列表已经包含：

```text
Claude Agent
Codex CLI
Cursor
OpenCode
OpenHands
Gemini CLI
Cline
Goose
Qwen Code
...
```

([Agent Client Protocol][16])

这意味着一个非常重要的未来架构：

```text
                 Agent Orchestrator
                         │
                     ACP / API
                         │
        ┌────────────────┼────────────────┐
        │                │                │
      Cursor          Codex          OpenCode
        │                │                │
      Claude           GPT             ...
```

你不需要让 Orchestrator 理解每个工具的内部实现。

---

# 二十五、所以我推荐“协议优先”

不要设计：

```text
Mastra → Cursor API
Mastra → Codex API
Mastra → OpenCode API
Mastra → WorkBuddy API
```

这种硬编码架构。

应该设计：

```text
                  Agent Interface
                        │
        ┌───────────────┼───────────────┐
        │               │               │
       ACP             API             CLI
        │               │               │
     Cursor           Agent X       OpenCode
     Codex             Agent Y       WorkBuddy
```

这样以后增加：

```text
Gemini CLI
Cline
Goose
Qwen Code
OpenHands
Kiro
```

都不会重写 Orchestrator。

---

# 二十六、你真正应该做的不是“AI员工”

而是：

# AI Employee Runtime

每一个员工都应该具有：

```text
Identity
Role
Skills
Tools
Workspace
Permissions
Memory
Task Queue
Input Contract
Output Contract
Review Policy
Retry Policy
```

例如：

```yaml
agent:
  id: frontend-agent

role: frontend-engineer

runtime: codex

skills:
  - react
  - typescript
  - nextjs
  - playwright

tools:
  - filesystem
  - git
  - browser
  - npm

permissions:
  read:
    - docs/**
    - backend/contracts/**
  
  write:
    - frontend/**
    - tests/frontend/**

cannot:
  - modify_database
  - modify_backend
  - deploy_production
```

这才是真正的“AI 员工”。

---

# 二十七、这样可以做到权限隔离

比如：

### UI Agent

```text
READ
docs/*
frontend/components/*

WRITE
frontend/ui/*
docs/design/*
```

不能：

```text
修改数据库
修改后端
发布生产
```

### Backend Agent

```text
WRITE
backend/*
database/*
contracts/*
```

不能：

```text
修改 UI
直接发布生产
```

---

# 二十八、再加 Git Worktree

多 Agent 并行最容易出现的问题：

```text
Agent A
修改 package.json

Agent B
也修改 package.json

Agent C
修改同一个组件
```

最终：

```text
Merge Conflict
```

所以每个任务必须有自己的：

```text
Git Worktree
```

例如：

```text
main
 │
 ├── task/UI-001
 ├── task/FE-001
 ├── task/BE-001
 └── task/DATA-001
```

完成后：

```text
Worker
 ↓
Commit
 ↓
PR
 ↓
CI
 ↓
Review
 ↓
Merge
```

Cursor 的 Cloud Agent 已经采用独立远程环境 + 独立分支的模式。([Cursor Documentation][17])

Claude Code 也提供 Worktree 来隔离并行任务。([Claude][18])

---

# 二十九、因此你的全自动闭环应该长这样

```text
                  PROJECT DOCS
                       │
                       ▼
              ┌────────────────┐
              │ Technical Lead │
              └───────┬────────┘
                      │
                      ▼
                 Task Planner
                      │
                      ▼
                  Task DAG
                      │
           ┌──────────┼──────────┐
           │          │          │
           ▼          ▼          ▼
          UI          DB         API
           │          │          │
           │          └────┬─────┘
           │               │
           │               ▼
           │            Backend
           │               │
           └──────┬────────┘
                  │
                  ▼
               Frontend
                  │
                  ▼
                Data
                  │
                  ▼
              Integration
                  │
                  ▼
                 QA
                  │
          ┌───────┴────────┐
          │                │
        PASS              FAIL
          │                │
          ▼                ▼
       Review          Diagnose
          │                │
          │                ▼
          │           Create Bug
          │                │
          │                ▼
          │           Assign Agent
          │                │
          │                ▼
          │             Rework
          │                │
          │                ▼
          │              Test
          │                │
          └────────────────┘
```

---

# 三十、这里有一个很重要的升级

你原来的模型是：

```text
技术经理
 ↓
员工
 ↓
提交
 ↓
技术经理检查
```

我建议改成：

```text
技术经理
 ↓
员工
 ↓
Automated QA
 ↓
Specialist Reviewer
 ↓
Technical Manager
 ↓
Merge
```

也就是：

> **不要让技术经理亲自发现所有错误。**

让机器先筛。

---

# 三十一、三级 Review

## Level 1：机器

```text
lint
typecheck
unit test
integration test
e2e
build
security
dependency
```

---

## Level 2：专业 Agent

例如：

```text
Frontend Reviewer
Backend Reviewer
UI Reviewer
Security Reviewer
```

---

## Level 3：Technical Manager

只处理：

```text
架构
跨模块
重大缺陷
需求偏差
技术决策
```

这样才能规模化。

---

# 三十二、为什么必须这么做？

因为多 Agent 最大的问题是：

# Coordination Cost

Claude Code 官方自己也明确指出，Agent Teams 虽然适合复杂并行工作，但会产生更高的协调开销和 Token 成本；对于高度依赖、修改同一文件的任务，单 Agent 或 Subagent 反而更合适。([Claude][6])

所以：

> **不是 Agent 越多越好。**

应该：

```text
任务可拆
    ↓
并行

任务有依赖
    ↓
串行

任务修改同文件
    ↓
避免并行

任务需要讨论
    ↓
Agent Team

任务只需要结果
    ↓
Subagent
```

---

# 三十三、因此我推荐“动态 Agent 数量”

不要固定：

```text
4 个员工
```

而是：

```text
任务 DAG
   ↓
计算并行度
   ↓
动态创建 Agent
```

例如：

```text
小项目

Technical Manager
       ↓
UI
Backend
       ↓
Frontend
       ↓
QA
```

大型项目：

```text
Technical Manager
       │
       ├── UI Lead
       │    ├── Layout
       │    ├── Components
       │    └── Visual QA
       │
       ├── Backend Lead
       │    ├── API
       │    ├── DB
       │    └── Security
       │
       ├── Frontend Lead
       │    ├── Pages
       │    ├── State
       │    └── E2E
       │
       └── Data Lead
            ├── API Client
            └── Integration
```

---

# 三十四、OpenHands 给了一个非常重要的工程方向

OpenHands 当前已经把：

```text
Agent
+
Server
+
Workspace
+
Automation
+
Multiple Backends
```

组合起来。

其 Agent Canvas 可以作为自托管控制中心，并可以运行 OpenHands、Claude Code、Codex 等不同 Agent 后端。([GitHub][3])

因此你完全可以借鉴：

# OpenHands = Worker Runtime / Agent Gateway

而不是重新造所有底层执行能力。

---

# 三十五、Mastra 在你的体系里更适合做什么？

结合你之前已经确定的：

> **Mastra = 发动机**

我仍然认为这个方向成立。

Mastra Workflow 的定位非常适合：

```text
Orchestration
State
Sequential
Parallel
Branch
Loop
Pause
Resume
Nested Workflow
```

([Mastra][8])

所以：

```text
Mastra
    ↓
Workflow Engine
    ↓
Task Scheduler
    ↓
Agent Router
    ↓
Worker
```

非常合适。

---

# 三十六、最终架构，我建议这样定

```text
┌───────────────────────────────────────────────┐
│                AI SOFTWARE FACTORY            │
├───────────────────────────────────────────────┤
│                                               │
│                 Project Domain                │
│                                               │
│  PRD / Architecture / Design / API / DB       │
│                                               │
├───────────────────────────────────────────────┤
│                                               │
│              Orchestration Layer              │
│                                               │
│                    Mastra                     │
│                                               │
│   Workflow / State / DAG / Scheduler         │
│                                               │
├───────────────────────────────────────────────┤
│                                               │
│              Management Layer                 │
│                                               │
│            Technical Manager                  │
│                  Cursor                       │
│                                               │
├───────────────────────────────────────────────┤
│                                               │
│               Agent Runtime                   │
│                                               │
│  UI       Frontend      Data      Backend     │
│  │           │           │          │         │
│Claude       Codex     WorkBuddy   OpenCode    │
│                                               │
├───────────────────────────────────────────────┤
│                                               │
│             Verification Layer                │
│                                               │
│   Test / E2E / Visual / Security / Review     │
│                                               │
├───────────────────────────────────────────────┤
│                                               │
│               Infrastructure                 │
│                                               │
│ Git / Worktree / CI / Docker / Sandbox        │
│ MCP / ACP / API / Event Bus                   │
│                                               │
└───────────────────────────────────────────────┘
```

---

# 三十七、我建议你不要直接照搬任何一个开源项目

这是非常重要的。

| 项目                        | 最值得借鉴                              |
| ------------------------- | ---------------------------------- |
| MetaGPT                   | 软件公司 / SOP / Role                  |
| ChatDev                   | Agent Communication                |
| AgentCoder                | Test → Feedback → Repair           |
| SWE-agent                 | Agent-Computer Interface           |
| OpenHands                 | Agent Runtime / Server / Workspace |
| AutoGen                   | Multi-Agent Communication          |
| Microsoft Agent Framework | Production Workflow                |
| Claude Agent Teams        | Parallel Team                      |
| Cursor Cloud Agents       | 长时间异步执行                            |
| Codex                     | 多 Agent 并行开发                       |
| ACP                       | Agent ↔ Coding Tool                |
| MCP                       | Agent ↔ Tools/Data                 |
| Mastra                    | Workflow / State / Orchestration   |

**不要选择其中一个，然后把整个系统建立在它上面。**

应该：

> **吸收它们的优秀架构，构建自己的 Domain。**

这与你之前对 CTXHS 的：

> **Mastra 是发动机，Domain 是车身**

其实完全一致。

---

# 三十八、论文还揭示了一个非常重要的问题：验证本身可能是假的

这一点非常值得警惕。

SWE-bench+ 的研究对 SWE-bench 的成功案例进行了人工审查，发现：

* 一部分成功 patch 存在 solution leakage；
* 一部分测试用例本身不足以验证正确性；
* 在过滤掉这些问题后，某些 Agent 的实际解决率会显著下降。

([arXiv][19])

这意味着：

> **“测试通过 ≠ 项目正确。”**

因此你的 AI 软件工厂绝不能：

```text
Test PASS
↓
DONE
```

而应该：

```text
Test
+
Contract
+
Review
+
Behavior
+
Regression
+
Security
+
Requirement Traceability
```

---

# 三十九、所以必须建立“需求追踪矩阵”

例如：

```text
REQ-001
 ↓
TASK-001
 ↓
UI-001
 ↓
FE-001
 ↓
API-001
 ↓
BE-001
 ↓
TEST-001
 ↓
E2E-001
```

最终技术经理可以问：

> “PRD 中的每一条需求是否都有实现和测试证据？”

这比单纯的代码 Review 高一个层级。

---

# 四十、全自动系统最终应该以“证据”驱动

每个任务必须产生：

```text
Task
 ↓
Implementation
 ↓
Test
 ↓
Evidence
 ↓
Review
 ↓
Decision
```

而不是：

```text
Agent says:
“Done.”
```

这是从“AI Demo”走向“工程系统”的关键。

---

# 四十一、最终的自动运行机制

用户只做：

```bash
factory start
```

系统：

```text
读取项目文档
        ↓
分析项目
        ↓
生成 Architecture
        ↓
生成 Task DAG
        ↓
计算并行任务
        ↓
启动 Agent
        ↓
Agent 工作
        ↓
提交 Artifact
        ↓
自动测试
        ↓
自动 Review
        ↓
发现问题？
     ┌──┴──┐
    YES    NO
     │      │
     ▼      ▼
  创建Bug   Merge
     │      │
     ▼      ▼
重新派工   下一阶段
     │
     ▼
  自动修复
     │
     ▼
  自动测试
     │
     └────→ 循环
```

直到：

```text
PROJECT_STATUS = COMPLETED
```

---

# 四十二、甚至可以做到断点恢复

这是我认为你必须设计的。

比如运行到：

```text
Frontend TASK-31
Backend TASK-28
UI TASK-25
```

电脑关机。

第二天：

```bash
factory resume
```

系统读取：

```text
project_state
task_state
agent_state
git_state
artifact_state
test_state
```

然后恢复：

```text
TASK-25 → COMPLETED
TASK-28 → RUNNING
TASK-31 → WAITING
```

继续运行。

这就是为什么必须有：

> **Persistent State Machine**

而不能靠某个 Cursor 对话窗口记忆。

---

# 四十三、最终可以形成真正的“无人值守”

例如：

```text
00:00
启动项目

00:01
Technical Manager

00:05
生成 83 个 Task

00:06
启动 12 个 Agent

00:40
UI 完成

00:42
DB 完成

01:10
Backend 完成

01:18
Frontend 完成

01:25
Integration

01:30
E2E FAIL

01:31
自动诊断

01:32
Backend Rework

01:48
PASS

01:50
Security FAIL

01:51
Security Fix

02:10
PASS

02:12
Final Review

02:15
Release
```

这才是你真正想要的：

# Autonomous Software Development Loop

---

# 四十四、但我不会承诺“真正 100% 无人”

这里必须非常客观。

目前可以追求：

> **长时间无人值守 + 自动规划 + 自动开发 + 自动测试 + 自动修复 + 自动迭代**

但不能把：

```text
100% 无人工监督
```

当成工程保证。

尤其涉及：

```text
生产数据库
支付
权限
Secret
生产部署
基础设施
高风险外部 API
```

最好设置：

```text
AUTO
   │
   ├── LOW RISK → 自动
   │
   ├── MEDIUM → 自动 + 审计
   │
   └── HIGH RISK → Approval Gate
```

MCP 的官方规范也强调，涉及工具调用时应该保留能够拒绝调用的人工控制能力。([Model Context Protocol][14])

---

# 四十五、我最终推荐的技术栈

如果让我现在为你真正落地，我会选：

| 层                 | 推荐                                |
| ----------------- | --------------------------------- |
| 产品文档              | Markdown                          |
| 项目规则              | AGENTS.md                         |
| Orchestrator      | **Mastra**                        |
| Workflow          | **Mastra Workflow**               |
| Project State     | PostgreSQL                        |
| Task State        | PostgreSQL                        |
| Event Bus         | Redis / NATS                      |
| Repository        | GitHub                            |
| Isolation         | Git Worktree + Docker             |
| UI Agent          | Claude Code                       |
| Frontend Agent    | Codex                             |
| Backend Agent     | OpenCode                          |
| Data Agent        | WorkBuddy                         |
| Technical Manager | Cursor / 独立 Manager Agent         |
| Worker Runtime    | OpenHands SDK 可重点研究               |
| Agent ↔ Tool      | MCP                               |
| Editor ↔ Agent    | ACP                               |
| API Contract      | OpenAPI                           |
| Browser Test      | Playwright                        |
| Unit              | Vitest / Jest / pytest            |
| E2E               | Playwright                        |
| Security          | Semgrep / Trivy / dependency scan |
| CI                | GitHub Actions                    |
| Observability     | OpenTelemetry                     |
| Artifact          | Git + `.ai/`                      |
| Task DAG          | 自建 Domain                         |
| Human Approval    | 高风险 Gate                          |

---

# 四十六、尤其推荐“OpenHands + Mastra”组合研究

不是：

```text
Mastra vs OpenHands
```

而是：

```text
Mastra
   ↓
Orchestrator
   ↓
OpenHands / ACP
   ↓
Coding Agents
```

也就是：

### Mastra

负责：

```text
WHY
WHAT
WHEN
WHO
DEPENDENCY
STATE
```

### Worker Runtime

负责：

```text
HOW
```

例如：

```text
Mastra：

“实现 TASK-042。”

        ↓

Worker Runtime：

“读取代码 → 修改 → 测试 → 提交。”
```

这是非常干净的职责分离。

---

# 四十七、而 Cursor 不应该成为整个系统的核心基础设施

这是我对你最初方案最大的修正。

你说：

> “使用 Cursor 作为技术经理。”

**可以。**

但应该理解为：

```text
Cursor = Technical Manager 的工作界面/执行环境
```

而不是：

```text
Cursor = 整个 AI 软件工厂
```

否则会形成厂商锁定。

---

# 四十八、真正的“技术经理”应该是你自己定义的 Agent

例如：

```text
TechnicalManagerAgent
```

它拥有：

```text
tools:
  create_task
  update_task
  assign_agent
  inspect_repository
  inspect_test
  inspect_pr
  request_rework
  approve
  reject
  merge
  rollback
```

它的系统规则：

```text
Never directly implement large features.

Always:
1. understand requirements
2. create task
3. determine dependency
4. assign worker
5. verify artifact
6. inspect evidence
7. request rework if needed
8. approve only when acceptance criteria pass
```

这才是你的：

# AI Technical Manager

---

# 四十九、最终可以进一步升级为“AI 公司操作系统”

最终：

```text
                    AI Company OS
                          │
              ┌───────────┴───────────┐
              │                       │
        Software Factory         Knowledge
              │                       │
        ┌─────┼─────┐                 │
        │     │     │                 │
       PM    CTO    QA              RAG
        │     │     │                 │
        └─────┼─────┘                 │
              │                       │
          Engineering                │
              │                       │
      ┌───────┼────────┐              │
      │       │        │              │
     UI    Frontend  Backend          │
      │       │        │              │
      └───────┼────────┘              │
              │                       │
              └───────────┬───────────┘
                          │
                       Release
```

这就已经不是一个：

> “AI 写代码工具”

而是：

> **AI Software Company Operating System。**

---

# 五十、我的最终判断

综合：

* MetaGPT
* ChatDev
* AgentCoder
* SWE-agent
* SWE-bench
* OpenHands
* Claude Agent Teams
* Cursor Multi-Agent
* Codex Multi-Agent
* MCP
* ACP
* Mastra
* Microsoft Agent Framework

可以得到一个非常明确的结论：

### 可行性：★★★★☆

**非常可行，但不能靠“多个 AI 工具互相聊天”实现。**

### 技术成熟度：

| 能力             | 当前成熟度 |
| -------------- | ----: |
| 单 Agent Coding | ★★★★★ |
| 多 Agent 并行     | ★★★★☆ |
| Git 隔离         | ★★★★★ |
| 自动测试           | ★★★★★ |
| 自动 Bug 修复      | ★★★★☆ |
| Task DAG       | ★★★★☆ |
| Agent 间通信      | ★★★★☆ |
| 长时间运行          | ★★★★☆ |
| 自动恢复           | ★★★☆☆ |
| 自动需求分析         | ★★★★☆ |
| 自动架构设计         | ★★★☆☆ |
| 自动 UI 开发       | ★★★★☆ |
| 完全无人监督         | ★★☆☆☆ |
| 自主完成复杂大型项目     | ★★★☆☆ |

所以真正现实的目标应该是：

# Level 3：Autonomous Software Factory

```text
完整项目文档
      ↓
AI Technical Manager
      ↓
自动拆解
      ↓
自动生成 DAG
      ↓
自动派工
      ↓
并行 Agent
      ↓
自动提交
      ↓
自动测试
      ↓
自动 Review
      ↓
自动发现问题
      ↓
自动创建修复任务
      ↓
自动重新派工
      ↓
自动回归
      ↓
自动合并
      ↓
自动发布
      ↓
项目完成
```

**这条路线现在已经具备实际工程落地条件。**

而你之前确定的 **“Mastra + Domain”** 路线，我建议继续保留，并把它升级成：

> **Mastra = Orchestration Engine**
> **Domain = AI Software Factory 核心业务层**
> **ACP/MCP = Agent/Tool 通信层**
> **OpenHands/Agent Runtime = Worker 执行层**
> **Cursor/Claude Code/Codex/WorkBuddy/OpenCode = 专业 Worker**
> **Git/CI/Playwright = 工程事实与验收层**

这会比“选一个多 Agent 框架直接套进去”更有生命力。

---

## 下一步我建议直接进入“工程设计阶段”

既然你已经明确说**“充分明确需求、准备好了完整项目文档体系之后，再让 AI 全自动开发”**，那么下一步最值得做的不是继续比较几个 Coding Agent，而是把这套体系正式规格化。

我建议下一轮直接给你产出一套：

```text
AI-SOFTWARE-FACTORY/
│
├── 00-系统总体架构.md
├── 01-核心设计原则.md
├── 02-Agent角色体系.md
├── 03-Agent Protocol.md
├── 04-Task Protocol.md
├── 05-Task DAG.md
├── 06-Project State Machine.md
├── 07-Agent Runtime.md
├── 08-MCP架构.md
├── 09-ACP架构.md
├── 10-Git Worktree协议.md
├── 11-Code Review协议.md
├── 12-QA协议.md
├── 13-Bug Rework协议.md
├── 14-自动恢复协议.md
├── 15-权限与Sandbox.md
├── 16-成本控制.md
├── 17-Observability.md
├── 18-Mastra Orchestrator.md
├── 19-Technical Manager.md
├── 20-UI Agent.md
├── 21-Frontend Agent.md
├── 22-Data Agent.md
├── 23-Backend Agent.md
├── 24-QA Agent.md
├── 25-Security Agent.md
├── 26-完整生命周期.md
├── 27-无人值守运行.md
├── AGENTS.md
└── README.md
```

然后我们再把它进一步压缩成**一套可以直接交给 Cursor/Codex/Claude Code/OpenCode 开始构建“AI 软件工厂本身”的实施计划**。

这一步，我认为才是整个项目真正的起点。

[1]: https://github.com/foundationagents/metagpt?utm_source=chatgpt.com "GitHub - FoundationAgents/MetaGPT: 🌟 The Multi-Agent Framework: First AI Software Company, Towards Natural Language Programming · GitHub"
[2]: https://arxiv.org/abs/2307.07924?utm_source=chatgpt.com "ChatDev: Communicative Agents for Software Development"
[3]: https://github.com/OpenHands/openhands?utm_source=chatgpt.com "GitHub - OpenHands/OpenHands: 🙌 OpenHands: AI-Driven Development · GitHub"
[4]: https://prod.cursor.com/help/ai-features/multi-agent?utm_source=chatgpt.com "What is multi-agent coding? | Cursor Docs"
[5]: https://openai.com/index/introducing-the-codex-app/?utm_source=chatgpt.com "Introducing the Codex app | OpenAI"
[6]: https://code.claude.com/docs/en/agent-teams?utm_source=chatgpt.com "Orchestrate teams of Claude Code sessions - Claude Code Docs"
[7]: https://arxiv.org/abs/2308.00352?utm_source=chatgpt.com "MetaGPT: Meta Programming for A Multi-Agent Collaborative Framework"
[8]: https://mastra.ai/ai-workflows?utm_source=chatgpt.com "Orchestrate Multi-Step AI Processes | Mastra"
[9]: https://github.com/OpenHands/software-agent-sdk/?utm_source=chatgpt.com "GitHub - OpenHands/software-agent-sdk: A clean, modular SDK for building AI agents with OpenHands V1. · GitHub"
[10]: https://github.com/princeton-nlp/SWE-agent/blob/main/docs/background/index.md?utm_source=chatgpt.com "SWE-agent/docs/background/index.md at main · SWE-agent/SWE-agent · GitHub"
[11]: https://github.com/microsoft/autogen?utm_source=chatgpt.com "GitHub - microsoft/autogen: A programming framework for agentic AI · GitHub"
[12]: https://github.com/microsoft/agent-framework?utm_source=chatgpt.com "GitHub - microsoft/agent-framework: A framework for building, orchestrating and deploying AI agents and multi-agent workflows with support for Python and .NET. · GitHub"
[13]: https://arxiv.org/abs/2312.13010?utm_source=chatgpt.com "AgentCoder: Multi-Agent-based Code Generation with Iterative Testing and Optimisation"
[14]: https://modelcontextprotocol.io/specification/2025-06-18/server/tools?utm_source=chatgpt.com "Tools - Model Context Protocol"
[15]: https://github.com/agentclientprotocol/agent-client-protocol?utm_source=chatgpt.com "GitHub - agentclientprotocol/agent-client-protocol: A protocol for connecting any editor to any agent · GitHub"
[16]: https://agentclientprotocol.com/get-started/agents?utm_source=chatgpt.com "Agents - Agent Client Protocol"
[17]: https://docs.cursor.com/background-agent?utm_source=chatgpt.com "Cursor – Background Agents"
[18]: https://code.claude.com/docs/en/agents?utm_source=chatgpt.com "Run agents in parallel - Claude Code Docs"
[19]: https://arxiv.org/abs/2410.06992?utm_source=chatgpt.com "SWE-Bench+: Enhanced Coding Benchmark for LLMs"
