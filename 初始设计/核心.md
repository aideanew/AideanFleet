

## 假设一(通过Hermes或CLI启动流程)
"项目需求文档已经在某某路径备好,使用某某端口,你需要逐一完成,直到达成我所有需求."


## 假设二(通过Hermes或CLI启动流程)
"实现我的以下需求:xxxxxxx."


## 假设三(通过Hermes或CLI启动流程)
"检查项目遗漏,使用hermes流程完成测试和修复."


## 假设四(通过Hermes或CLI启动流程)
"和我讨论,完成某某项目的需求分析.项目路径在xxxxx."  //这里的项目需要多agents一起开会讨论,比如claudecode作前端,codex作后端,opencode作产品.


## 假设五(通过Hermes或CLI启动流程)
"""
你必须自己选择合适的模型配置角色,统一协调各角色完成本项目:
## AMD （有限额度，使用优先级=2）
base_url = "https://developer.amd.com.cn/radeon/api/v1"
model_id = "DeepSeek-V4-Flash"  // 或者"Qwen3.8-Flash-Next"

## ModelScope  （有限额度，使用优先级=3）
base_url = "https://api-inference.modelscope.cn/v1"
model_id = "ZhipuAI/GLM-5.2" // 或者"ZhipuAI/GLM-4.7-Flash"
api_key = "${MODELSCOPE_API_KEY}"
api_key = "${AMD_API_KEY}"
"""

## 核心1
补充:
1. 模型限额自动切换模型;
2. 控制/展示界面需要实时同步;
3. 需要实时告诉用户本项目进度;

## 核心2
- (1)模型阻塞或报错后，如429限速，默认10秒重试一次，默认重试10次。如果10次后仍然报错，则需要切换模型。
- (2)我需要程序全部执行完毕后,输出调用了哪些角色,角色使用了什么平台,角色使用了什么模型,角色完成了哪些任务,角色表现评分.
| 角色 | 平台 | 模型 | 任务 | 评分 |
| 开发者1 | modelscope | deepseep-v4-flash | xxxxxx | xxxx |

## 核心3
1. 本流程系统内,没有教如何启动Hermes和CLI吗,感觉每次都要试错.
都以最高权限启动,示例:claude --dangerously-skip-permissions  
2. 本次测试模型,不允许使用GPT6,正式环境的提示词才可以.测试环境的最高管理员你使用agnes-3.0-flash吧
3. 先考虑下面两个问题,确保能覆盖实现.
"""
- (1)模型阻塞或报错后，如429限速，默认10秒重试一次，默认重试10次。如果10次后仍然报错，则需要切换模型。
- (2)我需要程序全部执行完毕后,输出调用了哪些角色,角色使用了什么平台,角色使用了什么模型,角色完成了哪些任务,角色表现评分.
| 角色 | 平台 | 模型 | 任务 | 评分 |
| 开发者1 | modelscope | deepseep-v4-flash | xxxxxx | xxxx |
""""
4. 当前我能提供的模型服务商包括:  //注意看过期时间和备注,不能用的不勉强.
"""
###  Cursor/WordBuddy/Zcode（你自己：额外的监督者，作为每个角色任务的检查和校验工作）  不影响该流程内部的监督者和审查者。

### V3 （额度有限。较昂贵的GPT中转，仅用于任务规划和管理）
base_url = "https://api.gpt.ge/v1"
model_id = "gpt-6-astra"
api_key = "${V3_API_KEY}"

### BAI（需要使用本次10808端口代理才能正常请求，使用优先级=1）
base_url = "https://api.b.ai/v1"
model_id = "qwen3.8-flash"
api_key = "${BAI_API_KEY}"

### Nvidia （使用优先级=3）
base_url = "https://integrate.api.nvidia.com/v1"
model_id = "nvidia/nemotron-3-ultra-550b-a55b"
api_key = "${NVIDIA_API_KEY}"

### Sensenova （使用优先级=4）
base_url = "https://token.sensenova.cn/v1"
model_id = "sensenova-6.8-flash-lite"
api_key = "${SENSNOVA_API_KEY}"


### agnes （使用优先级=5）
base_url = "https://apihub.agnes-ai.com/v1"
model_id = "agnes-3.0-flash
api_key = "${AGNES_API_KEY}"

### AMD （有限额度，使用优先级=2）
base_url = "https://developer.amd.com.cn/radeon/api/v1"
model_id = "DeepSeek-V4-Flash"  // 或者"Qwen3.8-Flash-Next"
api_key = "${AMD_API_KEY}"

### ModelScope  （有限额度，使用优先级=3）
base_url = "https://api-inference.modelscope.cn/v1"
model_id = "ZhipuAI/GLM-5.2" // 或者"ZhipuAI/GLM-4.7-Flash"
api_key = "${MODELSCOPE_API_KEY}"
"""

## 核心4
这大概率是实际运行中极少数需要用户确认的项。
如果hermes/端口或其他资源被占用，需要先问用户是否清理。用户只需要回答Y/N

## 安全说明（2026-09-15 修订）
原明文 Key 已外置为 ${VAR} 占位，不再记录真值；旧 Key 视为已泄露，请在各平台后台轮换。
真值只允许进入 secrets/.env 或 secret store，对应关系见 secrets/KEY_INVENTORY.md。
