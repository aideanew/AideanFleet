# AideanFleet 运行与测试依赖（CORE-03 固化，以 grep import 实测为准，非记忆）
# 验证环境：Windows 10 / Python 3.12.13 / 2026-09-15
fastapi==0.141.1          # fleet/console/server.py（Web 控制台）
uvicorn[standard]==0.53.0 # fleet/console/server.py main()（ASGI 服务）
websockets==17.1.0        # /ws 实时通道（uvicorn[standard] 亦携带，显式声明保证测试可用）
httpx==0.28.1             # fastapi.testclient（tests/core/test_server_api.py）运行时依赖
pytest==9.1.1             # 测试框架（python -m pytest tests -q）
