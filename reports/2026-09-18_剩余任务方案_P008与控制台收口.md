# 剩余任务方案大纲（P-008 / 控制台 8 项需求收口）

> 生成：2026-09-18 ｜ 依据：真实代码穿透 + data/events.jsonl + fleet.db + dist 产物核验
> 进度口径：与 fleet/core/plan.py compute_progress 一致，叶子权重合计恒 = 100%

## 当前进度快照

- 服务：127.0.0.1:5000 已重启（旧 PID 18144 → 新 PID 40948），/api/health 全绿。
- P-008（Test09181120）：3/5 DONE，T04/T05 BLOCKED（opencode 600s 超时 → 卡死升级），项目进度 60.0%。
- 关键断点：dist/ 构建于 15:37:27，web/src 最新改动至 15:59:59 —— 需求 4/5/6 的前端代码已写但**未进产物**（dist 中 grep `executors/running`、`history/tasks`、`roles/summary`、`未生成任务计划` 均零命中）。

---

## L1-A 数据与后端口径修复（权重 35%）

### L2-A1 执行体身份与计量口径统一（12%）
- [ ] L3-A1.1（4%）fleet.db tasks.adapter 回填：从 events `task:doing` extra.adapter 取值回填（当前 5 个任务该列全为空串 ""，证据：db_check_p008.py 输出）。
- [ ] L3-A1.2（4%）/api/history/tasks、/api/roles/*/profile 的 adapter 字段改为"DB 优先、事件流兜底"（server.py:1039 现直接读空列）。
- [ ] L3-A1.3（4%）usage 计量修复：T01 四项全 0（事件 1852）与 169KB 真实输出矛盾；opencode total_tokens 为会话累计（111910 ≫ 6880+1319）。统一为 prompt+completion+reasoning 口径，事件与 DB 双写一致。

### L2-A2 证据链落盘规范（11%）
- [ ] L3-A2.1（4%）evidence 布局改为 `data/projects/<pid>/evidence/<task_id>/attempt-N.log`（现状按适配器名归档且每次覆盖，无法按任务追溯）。
- [ ] L3-A2.2（4%）每次尝试独立文件，固定 `=== STDOUT/STDERR/EXCEPTION/TIMEOUT ===` 四段结构。
- [ ] L3-A2.3（3%）日志元数据头（task_id/role/attempt/started_at），杜绝 opencode/attempt-1.log 这类无法归属的孤儿日志（mtime 15:27:50 减 600s 无对应派工）。

### L2-A3 调度与卡死治理（12%）
- [ ] L3-A3.1（4%）opencode 600s 超时治理：心跳/部分输出检测，或按任务类型动态 timeout（T04/T05 连续两轮超时是本次卡死根因）。
- [ ] L3-A3.2（4%）model_switch 后成功调用必须落 model:call 事件（T04 切换链 modelscope→amd 后零调用，防御性复现检查）。
- [ ] L3-A3.3（4%）BLOCKED 任务进度语义固化：计入分母、不计完成、UI 显示"已停止"（现状 plan.py DONE_STATES 仅 DONE/PARTIAL，语义正确但前端无独立视觉态）。

## L1-B 前端展示与交互重建（权重 45%）

### L2-B1 构建与发布链路（6%）
- [ ] L3-B1.1（3%）重建 dist 并重启控制台（根因修复：所有"看不到"问题的共同前提）。
- [ ] L3-B1.2（3%）启动时校验 dist 与 src 新旧关系，落后则控制台横幅提示"前端产物过期"。

### L2-B2 项目名与任务计划可见性（8%）
- [ ] L3-B2.1（3%）TopBar 项目名回显走查（源码已有兜底 option：TopBar.vue:86-92 `auth.project || '未解锁'`）。
- [ ] L3-B2.2（3%）Overview 渲染 planState 五态文案（plan.ts:89-115 missing/empty/unplanned/planned/running，回答"是没指定还是没刷新"）。
- [ ] L3-B2.3（2%）进度栏标题展示 plan.json title（P-008 现有 `"title": "Test09181120"`）。

### L2-B3 颗粒化进度（10%）
- [ ] L3-B3.1（4%）进度栏渲染 stages 权重树（weight_pct/contrib_pct，后端 compute_progress 已保证叶子合计恒 100，Hamilton 舍入防 0.1% 漂移）。
- [ ] L3-B3.2（3%）阶段级百分比展示（P-008：技术设计 20% + 核心实现 40% = 60%，测试验收 0%）。
- [ ] L3-B3.3（3%）BLOCKED 任务独立视觉态（黄/红徽标 + "已停止"，与运行中区分）。

### L2-B4 执行体实时面板（8%）
- [ ] L3-B4.1（4%）ExecutorsPage 运行表重建后走查（PID/进程名/任务/角色/已运行/alive；源码已含 2s 自动刷新 pollTimer）。
- [ ] L3-B4.2（2%）adapter 列兜底显示（依赖 A1.2）。
- [ ] L3-B4.3（2%）跨进程调度器执行体可见性：页脚注明登记范围，或扩展 registry 跨进程登记。

### L2-B5 历史任务与角色档案（8%）
- [ ] L3-B5.1（4%）历史任务列表页走查（/api/history/tasks 字段齐全：执行/审查角色、耗时、token、返工次数；server.py:1010-1044）。
- [ ] L3-B5.2（4%）角色档案页走查（/api/roles/{role}/profile：executed/reviewed/future、耗时、token、call_count；server.py:1047-1113）。

### L2-B6 角色贡献重设计 + 全局自动刷新（5%）
- [ ] L3-B6.1（2%）角色贡献卡片重设计（按 token 排序、占比条、数字格式化；现状仅 onMounted + 手动刷新，OverviewPage.vue:50-58）。
- [ ] L3-B6.2（2%）WS 驱动自动刷新：task_update/progress 事件触发 roles summary 去抖刷新（复用 useWebSocket schedulePlanRefresh 300ms 模式）。
- [ ] L3-B6.3（1%）各页面刷新策略统一（ExecutorsPage 2s 轮询已有；历史/角色页接入同一机制）。

## L1-C 可靠性与验收闭环（权重 20%）

### L2-C1 P-008 项目收尾（8%）
- [ ] L3-C1.1（3%）T04/T05 人工释放（task:blocked_release）并重派（依赖 A3.1 超时治理先行，否则复现卡死）。
- [ ] L3-C1.2（3%）T05 判别分歧复核：report.md 首部 `## REWORK` 而 gate.json passed=true（db rework_count=1，retry_context.failure.review=REWORK）——明确以 review_verdict 为准并修正状态机记录。
- [ ] L3-C1.3（2%）gate 证据卫生：changed_files 排除 `"nul"`（Windows NUL 设备）与 playwright-report blob；evidence_dir 恒 null 修复。

### L2-C2 端到端验证（7%）
- [ ] L3-C2.1（4%）8 项需求逐条 UI 走查清单（对照本方案证据链，逐项留截图/断言）。
- [ ] L3-C2.2（3%）compute_progress 单测补 BLOCKED/孤儿任务/全 BLOCKED 用例，断言叶子权重合计恒 100%。

### L2-C3 治理与文档（5%）
- [ ] L3-C3.1（2%）验证报告缺陷项归档 issue 清单（usage 全 0、孤儿日志、evidence_dir null、adapter 空列）。
- [ ] L3-C3.2（2%）docs/契约/控制台API.md 补录三个新端点（executors/running、history/tasks、roles/{role}/profile）。
- [ ] L3-C3.3（1%）会话交接说明归档（本方案 + 分析报告）。

---

## 权重核对

| L1 | 权重 | L2 明细 |
|---|---|---|
| A 数据与后端 | 35% | A1 12 + A2 11 + A3 12 |
| B 前端重建 | 45% | B1 6 + B2 8 + B3 10 + B4 8 + B5 8 + B6 5 |
| C 验收闭环 | 20% | C1 8 + C2 7 + C3 5 |
| **合计** | **100%** | 全部 L3 原子任务权重和 = 100.0% |

## 执行顺序建议

B1.1（重建发布）→ B2/B3（可见性）→ A1（口径）→ B4/B5/B6（面板）→ A2/A3（治理）→ C1（P-008 收尾）→ C2/C3（验收归档）。
关键依赖：B4.2/B6 依赖 A1；C1.1 依赖 A3.1；C2 走查依赖 B1.1 完成。
