# Product Requirements · 当前需求基线

> 类型：requirement ｜ 状态：**partially-established** ｜ 更新：2026-09-22 ｜ 基线：HEAD `eb7a63c`
>
> 本文件保存**当前有效的正式需求基线**，只描述现状，不是历史需求的集合。
>
> ⚠️ **诚实声明**：本项目截至本文件建立时，**尚未建立正式 REQ 编号体系**。
> 实际需求以「工作包 + 证据索引」形式存在于执行方案中。下方"需求候选"是从既有权威文档
> **逐条抽取的候选清单**，尚未走 Inbox → 分流 → 编号流程。它们不是正式需求。

---

## 1. 需求事实的权威来源

| 层级 | 权威文件 | 承载内容 |
| --- | --- | --- |
| 原始用户需求 | `../../docs/参考/base.md` | 三级规划、双入口、七页面、对话中修正任务、Model/Role/Executor/Tools 分层、工具配置、提醒 |
| 当前执行方案 | `../../docs/plans/2026-09-17-evidence-driven-remaining-work.md` | E01–E20 证据索引 + W1–W10 工作包 + 每项验收条件 |
| 架构决策 | `../../docs/adr.md` | ADR-016~023（约束类需求的裁决结果） |
| 运行时契约 | `../../docs/契约/` 三份 FROZEN 文档 | 状态机 10 状态、15 契约字段、控制台 API |

⚠️ `DELIVERY/` 下三份讨论稿**不是**本项目的需求来源——它们的分析对象是
Aidean/AideanBot 兄弟项目（基线 `f1068e4`/`205c151`/`5b17da2`，均非本仓库 `eb7a63c`）。
详见 [`../00_governance/document-index.md`](../00_governance/document-index.md) 的说明。

---

## 2. 需求候选清单（从 W1–W8 抽取，待编号）

> 抽取自 `docs/plans/2026-09-17-evidence-driven-remaining-work.md` §3.1–3.8 与 §2 证据索引。
> 每条给出建议分类（F/NF/C）与建议编号区间，**待走 Inbox 流程后转正**。

### 候选组 1：可信启动与控制台可用性（W1，P0，角色 B/C，依据 E15/E17/E18）

| 候选 ID | 需求 | 建议分类 |
| --- | --- | --- |
| REQ-F-101 | 健康判定契约对齐：health 三项能力必须为布尔真，缺键/false/异常响应不可通过 | F |
| REQ-F-102 | 启动结果传播：每步记录真实成功/失败/降级；Hermes 不可用不伪装为可用 | F |
| REQ-F-103 | 端口单源：console/manager/project 三端口由同一配置对象解析 | F |
| REQ-F-104 | 前端新项目切换：受控会话刷新/切换，TTL 从服务端取剩余有效期 | F |
| REQ-F-105 | 统一工作区校验：入口复用规范化路径与根目录包含判定，空 allowlist 一致拒绝 | F |

### 候选组 2：唯一调度者与严格人工控制（W2，P0，角色 A，依据 E03/E04/E05）

| 候选 ID | 需求 | 建议分类 |
| --- | --- | --- |
| REQ-F-201 | 生命周期统一：同项目重复启动只存在一个有效调度者 | F |
| REQ-NF-202 | 数据库原子领取：同任务并发请求只获得一个 claim | NF |
| REQ-F-203 | 确认绑定对象：确认持久化 project/task/action/plan_version 与一次性消费状态 | F |
| REQ-F-204 | 意见不等同放行：note 写意见事件但不生成执行许可 | F |
| REQ-F-205 | 一次确认只走一个动作：step 路径不得调用内部自动返工多轮长闭环 | F |
| REQ-F-206 | 严格依赖关系：坏 JSON/未知依赖明确报错，显式 predecessor/successor | F |
| REQ-NF-207 | 产物生命周期保护：lease 覆盖执行/提交/审查直至产物固化 | NF |

### 候选组 3：状态、事件与计划一致性（W3，P0，角色 A，依据 E06/E12/E13）

| 候选 ID | 需求 | 建议分类 |
| --- | --- | --- |
| REQ-NF-301 | 事务内状态与待投递事件：SQLite 同事务写 task/attempt 与 outbox | NF |
| REQ-NF-302 | 可重入事件导出：故障点中断后 DB 事件与 JSONL 可逐条对账 | NF |
| REQ-F-303 | 计划结构入库：丢失快照可仅从 DB 重建完整三级顺序及状态 | F |
| REQ-NF-304 | 快照投影原子发布：快照版本单调、重建幂等 | NF |
| REQ-F-305 | 存储上下文贯通：指定非默认 db_file/数据根时默认库无写入 | F |

### 候选组 4：需求驱动规划与变更闭环（W4，P1，角色 A，依据 E01/E02）

| 候选 ID | 需求 | 建议分类 |
| --- | --- | --- |
| REQ-F-401 | 结构化需求契约：requirement/acceptance/risk/assignee/dependencies/deliverables 可校验 | F |
| REQ-F-402 | 可替换规划提供者：生产由 provider 返回任务建议，不再无条件固定 5 条 | F |
| REQ-F-403 | 计划变更命令：改需求生成变更提案 + 影响集合 + 待确认状态 | F |
| REQ-F-404 | 五工作模式执行策略：非执行模式的 adapter 写能力不可用 | F |
| REQ-NF-405 | 终局验收依赖：集成未 DONE 时终局不可 READY；禁止同一身份自审自交付 | NF |
| REQ-F-406 | 变更/暂停运行态语义：暂停期间零新增 claim，运行结果不被静默删除 | F |

### 候选组 5：可信验收与不可混淆的执行证据（W5，P0/P1，角色 A/C，依据 E07/E08/E02）

| 候选 ID | 需求 | 建议分类 |
| --- | --- | --- |
| REQ-NF-501 | 每轮独立证据：按 project/task/attempt 存基线与回执，首轮证据不被覆盖 | NF |
| REQ-NF-502 | 完整输出与摘要分离：全量 stdout/stderr 写文件，API 只读有界尾部 | NF |
| REQ-NF-503 | 证据写失败关闭验收：必需证据失败使结果不可通过 | NF |
| REQ-NF-504 | 需求级机器门：空 src 目录不能通过"可启动/业务正确"验收 | NF |
| REQ-F-505 | 审查结构与身份历史：单一 verdict，执行/审查各自 model/usage 可追溯 | F |
| REQ-NF-506 | 验证进程生命周期：超时/输出上限/子进程清理，失败保留退出事实 | NF |

### 候选组 6：计量、预算、审批真正进入执行路径（W6，P0/P1，角色 A，依据 E08~E11）

| 候选 ID | 需求 | 建议分类 |
| --- | --- | --- |
| REQ-NF-601 | 事件计量幂等：同事件扫描两次总量不变，轮转后仍可追踪 | NF |
| REQ-NF-602 | 持久消费检查点：无需手动 scan 即可查询用量，重启不漏不重 | NF |
| REQ-NF-603 | 完整调用计量：unknown 与 0 区分，缺 usage 不冒充免费 | NF |
| REQ-NF-604 | 预算预留与结算：并发消费不丢更新、不重复预留，余额不足 adapter 未调用 | NF |
| REQ-NF-605 | 审批绑定具体动作：批准只放行对应动作一次，拒绝/过期不放行 | NF |
| REQ-F-606 | 审批恢复与超时执行：批准预算申请须实际更新授权额度后再恢复 | F |

### 候选组 7：恢复、通知与可观察生命周期（W7，P1，角色 C/A，依据 E12/E13/E14）

| 候选 ID | 需求 | 建议分类 |
| --- | --- | --- |
| REQ-F-701 | 启动实际调用恢复：四态均从真实 startup 入口验证 | F |
| REQ-NF-702 | 优雅停止与租约回收：无重复消费者，未知结果进入待核验状态 | NF |
| REQ-F-703 | 通知使用事件身份：游标改 event.seq/id，改变 CWD 不改变数据位置 | F |
| REQ-F-704 | 统一通知配置源：fake sender 验证开关变更无需重启即可生效 | F |
| REQ-NF-705 | 通知结果可重试可去重：提供稳定消息标识与不确定状态 | NF |
| REQ-NF-706 | 后台错误可观察：暴露调度心跳、投影滞后与队列积压；基础健康不冒充完整就绪 | NF |

### 候选组 8：真实扩展能力与控制面边界（W8，P1，角色 A/B/C，依据 E16/E19/E20）

| 候选 ID | 需求 | 建议分类 |
| --- | --- | --- |
| REQ-F-801 | 执行体能力声明：模型覆盖/工具配置/流式/只读/取消，不支持时阻塞或显式降级 | F |
| REQ-NF-802 | 依赖口径统一：pyproject 与 requirements 对齐，形成干净安装出口 | NF |
| REQ-NF-803 | 检查出口统一：Python 测试 + 前端类型检查 + 端到端基线形成统一出口 | NF |
| REQ-NF-804 | 测试不能代替真实验收：保留浏览器与外部执行体验收路径 | NF |

---

## 3. 已由 ADR 裁决的约束类需求（可直接视为 `REQ-C`）

这些约束已有关键决策裁决，**不再需要重新讨论**，但可按需补建 `REQ-C-NNN` 以入链：

| 约束 | 裁决 | 状态 |
| --- | --- | --- |
| 核心代码不得 import 前端框架或构建工具 | [ADR-016](../../docs/adr.md) | ACCEPTED（继承 ADR-010 的边界原则） |
| UI = 控制台 = 5000，Manager 9900，项目 3333 | [ADR-017](../../docs/adr.md) | ACCEPTED |
| 派工默认单次、非交互执行 | [ADR-021](../../docs/adr.md) | ACCEPTED |
| 契约层冻结 + 单向依赖（executors → manager） | [ADR-022](../../docs/adr.md) | ACCEPTED |
| 状态机状态名与迁移方向冻结，扩展只能新增 | [任务状态机契约](../../docs/契约/任务状态机.md) FROZEN v1.1 | ACCEPTED |
| 项目名统一 AideanFleet | [ADR-023](../../docs/adr.md) | ACCEPTED |

---

## 4. 状态总览

| 项 | 状态 |
| --- | --- |
| 正式 REQ 编号体系 | 🚧 未建立 |
| 需求候选清单 | ✅ 本文件 §2，44 条候选 |
| 约束类需求（ADR 裁决） | ✅ 6 条，见 §3 |
| Inbox 目录 | ✅ 就位，无条目 |
| Backlog | ✅ 见 [`backlog.md`](./backlog.md) |

## 5. 更新规则

* 候选转正 → 走 Inbox 流程后，把对应行移到 §1 权威来源链上，并在 `features/`/`non-functional/`/`constraints/` 建正式文档
* 本文件**只描述当前有效状态**；被拒绝/取消的需求进 [`../09_archive/`](../09_archive/)
* 需求变化 → 同步更新本文件 + `backlog.md` + 关联 SOL/Plan/Task（见 [`../00_governance/document-standard.md`](../00_governance/document-standard.md) §7）
